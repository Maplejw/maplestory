.程序集 测谎对接, , , 

.子程序 PT_初始化, , 公开, 
.参数 线程索引, 整数型, , 
.参数 服务端IP, 文本型, , 
.参数 服务端端口, 整数型, , 
.参数 跟踪类型, 整数型, , 
.参数 模型类型, 整数型, , 
.参数 ROI左, 整数型, , 
.参数 ROI上, 整数型, , 
.参数 ROI右, 整数型, , 
.参数 ROI下, 整数型, , 

PT_断开 (线程索引)
赋值 (全_线程参数[线程索引].PT_服务端IP, 服务端IP)
赋值 (全_线程参数[线程索引].PT_服务端端口, 服务端端口)
赋值 (全_线程参数[线程索引].PT_跟踪类型, 跟踪类型)
赋值 (全_线程参数[线程索引].PT_模型类型, 模型类型)
赋值 (全_线程参数[线程索引].PT_ROI左, ROI左)
赋值 (全_线程参数[线程索引].PT_ROI上, ROI上)
赋值 (全_线程参数[线程索引].PT_ROI右, ROI右)
赋值 (全_线程参数[线程索引].PT_ROI下, ROI下)
赋值 (全_线程参数[线程索引].PT_已连接, 假)
赋值 (全_线程参数[线程索引].PT_套接字, 0)
赋值 (全_线程参数[线程索引].PT_序列号, 0)
赋值 (全_线程参数[线程索引].PT_有结果, 假)
赋值 (全_线程参数[线程索引].PT_最后错误, “”)
赋值 (全_线程参数[线程索引].PT_连接时间, 0)
赋值 (全_线程参数[线程索引].PT_已保存首帧, 假)
赋值 (全_线程参数[线程索引].PT_检测开始时间, 0)
调试输出 (线程索引, “PT初始化 线程=”, 线程索引, “IP=”, 服务端IP, “端口=”, 服务端端口, “类型=”, 跟踪类型, “模型=”, 模型类型)

.子程序 PT_应取消, 逻辑型, , 停止标志与单次测谎30秒总预算
.参数 线程索引, 整数型, ,

.如果真 (或者 (全_程序退出中, 全_请求停止所有, 全_线程参数[线程索引].停止, 全_线程停止标志[线程索引]))
    返回 (真)
.如果真结束
.如果真 (并且 (不等于 (全_线程参数[线程索引].PT_检测开始时间, 0), 大于或等于 (相减 (取启动时间 (), 全_线程参数[线程索引].PT_检测开始时间), 30000)))
    返回 (真)
.如果真结束
返回 (假)

' ================================================================
' 连接服务端
' ================================================================

.子程序 PT_连接, 逻辑型, 公开, 返回真=成功
.参数 线程索引, 整数型, , 
.局部变量 套接字, 整数型, , , 
.局部变量 服务端地址, 字节集, , , 
.局部变量 端口网络序, 整数型, , , 
.局部变量 IP整数, 整数型, , , 
.局部变量 超时毫秒, 整数型, , , 
.局部变量 wsaData, 字节集, , , 
.局部变量 nodelay, 整数型, , , 
.局部变量 非阻塞, 整数型, , ,
.局部变量 连接返回, 整数型, , ,
.局部变量 错误代码, 整数型, , ,
.局部变量 可写集合, 字节集, , ,
.局部变量 异常集合, 字节集, , ,
.局部变量 等待时间, 字节集, , ,
.局部变量 开始等待, 整数型, , ,
.局部变量 选择结果, 整数型, , ,
.局部变量 套接字错误, 整数型, , ,
.局部变量 选项长度, 整数型, , ,
.局部变量 连接成功, 逻辑型, , ,

.如果真 (PT_取连接状态 (线程索引))
    PT_断开 (线程索引)
.如果真结束
.如果真 (等于 (PT_WSA已初始化, 假))
    赋值 (wsaData, 取空白字节集 (512))
    .如果真 (不等于 (WSAStartup (514, wsaData), 0))
        赋值 (全_线程参数[线程索引].PT_最后错误, “WSA初始化失败”)
        返回 (假)
    .如果真结束
    赋值 (PT_WSA已初始化, 真)
.如果真结束
赋值 (套接字, socket (2, 1, 6))
.如果真 (等于 (套接字, -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “创建套接字失败”)
    返回 (假)
.如果真结束
赋值 (nodelay, 1)
.如果真 (等于 (setsockopt (套接字, 6, 1, nodelay, 4), -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “设置TCP_NODELAY失败”)
    closesocket (套接字)
    返回 (假)
.如果真结束
赋值 (超时毫秒, 2000)
.如果真 (等于 (setsockopt (套接字, 65535, 4102, 超时毫秒, 4), -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “设置接收超时失败”)
    closesocket (套接字)
    返回 (假)
.如果真结束
.如果真 (等于 (setsockopt (套接字, 65535, 4101, 超时毫秒, 4), -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “设置发送超时失败”)
    closesocket (套接字)
    返回 (假)
.如果真结束
赋值 (非阻塞, 1)
.如果真 (等于 (ioctlsocket (套接字, -2147195266, 非阻塞), -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “设置非阻塞连接失败”)
    closesocket (套接字)
    返回 (假)
.如果真结束
赋值 (服务端地址, 取空白字节集 (16))
赋值 (端口网络序, htons (全_线程参数[线程索引].PT_服务端端口))
赋值 (IP整数, inet_addr (全_线程参数[线程索引].PT_服务端IP))
PT_写整数 (服务端地址, 0, 2, 2)
PT_写整数 (服务端地址, 2, 端口网络序, 2)
PT_写整数 (服务端地址, 4, IP整数, 4)
线程_进入许可区 (全_线程参数[线程索引].PT_状态许可)
赋值 (全_线程参数[线程索引].PT_套接字, 套接字)
赋值 (全_线程参数[线程索引].PT_已连接, 假)
线程_退出许可区 (全_线程参数[线程索引].PT_状态许可)
.如果真 (PT_应取消 (线程索引))
    赋值 (全_线程参数[线程索引].PT_最后错误, “连接已取消”)
    PT_断开 (线程索引)
    返回 (假)
.如果真结束
赋值 (连接成功, 假)
赋值 (连接返回, connect (套接字, 服务端地址, 16))
.如果 (等于 (连接返回, 0))
    赋值 (连接成功, 真)
.否则
    赋值 (错误代码, WSAGetLastError ())
    .如果真 (并且 (不等于 (错误代码, 10035), 不等于 (错误代码, 10036), 不等于 (错误代码, 10037)))
        赋值 (全_线程参数[线程索引].PT_最后错误, 相加 (“连接失败:”, 到文本 (错误代码)))
        PT_断开 (线程索引)
        返回 (假)
    .如果真结束
    赋值 (开始等待, 取启动时间 ())
    .判断循环首 (小于 (相减 (取启动时间 (), 开始等待), 1200))
        .如果真 (或者 (PT_应取消 (线程索引), 不等于 (PT_取套接字 (线程索引), 套接字)))
            赋值 (全_线程参数[线程索引].PT_最后错误, “连接已取消”)
            PT_断开 (线程索引)
            返回 (假)
        .如果真结束
        赋值 (可写集合, 取空白字节集 (260))
        赋值 (异常集合, 取空白字节集 (260))
        赋值 (等待时间, 取空白字节集 (8))
        PT_写整数 (可写集合, 0, 1, 4)
        PT_写整数 (可写集合, 4, 套接字, 4)
        PT_写整数 (异常集合, 0, 1, 4)
        PT_写整数 (异常集合, 4, 套接字, 4)
        PT_写整数 (等待时间, 0, 0, 4)
        PT_写整数 (等待时间, 4, 100000, 4)
        赋值 (选择结果, select_ (0, 0, 取变量数据地址 (可写集合), 取变量数据地址 (异常集合), 取变量数据地址 (等待时间)))
        .如果真 (大于 (选择结果, 0))
            赋值 (套接字错误, 0)
            赋值 (选项长度, 4)
            .如果真 (等于 (getsockopt (套接字, 65535, 4103, 套接字错误, 选项长度), 0))
                .如果 (等于 (套接字错误, 0))
                    赋值 (连接成功, 真)
                .否则
                    赋值 (全_线程参数[线程索引].PT_最后错误, 相加 (“连接失败:”, 到文本 (套接字错误)))
                .如果结束
            .如果真结束
            跳出循环 ()
        .如果真结束
        .如果真 (等于 (选择结果, -1))
            赋值 (全_线程参数[线程索引].PT_最后错误, 相加 (“连接等待失败:”, 到文本 (WSAGetLastError ())))
            跳出循环 ()
        .如果真结束
    .判断循环尾 ()
.如果结束
.如果真 (等于 (连接成功, 假))
    .如果真 (等于 (全_线程参数[线程索引].PT_最后错误, “”))
        赋值 (全_线程参数[线程索引].PT_最后错误, “连接超时”)
    .如果真结束
    PT_断开 (线程索引)
    返回 (假)
.如果真结束
赋值 (非阻塞, 0)
.如果真 (等于 (ioctlsocket (套接字, -2147195266, 非阻塞), -1))
    赋值 (全_线程参数[线程索引].PT_最后错误, “恢复阻塞模式失败”)
    PT_断开 (线程索引)
    返回 (假)
.如果真结束
线程_进入许可区 (全_线程参数[线程索引].PT_状态许可)
.如果 (并且 (等于 (全_线程参数[线程索引].PT_套接字, 套接字), 等于 (PT_应取消 (线程索引), 假)))
    赋值 (全_线程参数[线程索引].PT_已连接, 真)
.否则
    赋值 (连接成功, 假)
.如果结束
线程_退出许可区 (全_线程参数[线程索引].PT_状态许可)
.如果真 (等于 (连接成功, 假))
    PT_断开 (线程索引)
    返回 (假)
.如果真结束
赋值 (全_线程参数[线程索引].PT_序列号, 0)
赋值 (全_线程参数[线程索引].PT_有结果, 假)
赋值 (全_线程参数[线程索引].PT_最后错误, “”)
赋值 (全_线程参数[线程索引].PT_连接时间, 取启动时间 ())
调试输出 (线程索引, “PT连接成功 套接字=”, 套接字)
返回 (真)

' ================================================================
' 断开连接
' ================================================================

.子程序 PT_断开, , 公开, 
.参数 线程索引, 整数型, , 
.局部变量 待关闭套接字, 整数型, , ,

线程_进入许可区 (全_线程参数[线程索引].PT_状态许可)
赋值 (待关闭套接字, 全_线程参数[线程索引].PT_套接字)
赋值 (全_线程参数[线程索引].PT_套接字, 0)
赋值 (全_线程参数[线程索引].PT_已连接, 假)
赋值 (全_线程参数[线程索引].PT_有结果, 假)
线程_退出许可区 (全_线程参数[线程索引].PT_状态许可)
.如果真 (不等于 (待关闭套接字, 0))
    closesocket (待关闭套接字)
.如果真结束

.子程序 PT_取套接字, 整数型, , 只在短临界区读取套接字句柄
.参数 线程索引, 整数型, ,
.局部变量 套接字, 整数型, , ,

线程_进入许可区 (全_线程参数[线程索引].PT_状态许可)
赋值 (套接字, 全_线程参数[线程索引].PT_套接字)
线程_退出许可区 (全_线程参数[线程索引].PT_状态许可)
返回 (套接字)

.子程序 PT_取连接状态, 逻辑型, , 只在短临界区读取连接状态
.参数 线程索引, 整数型, ,
.局部变量 已连接, 逻辑型, , ,

线程_进入许可区 (全_线程参数[线程索引].PT_状态许可)
赋值 (已连接, 全_线程参数[线程索引].PT_已连接)
线程_退出许可区 (全_线程参数[线程索引].PT_状态许可)
返回 (已连接)

' ================================================================
' 检查重连（单连接最长60秒）
' ================================================================

.子程序 PT_检查重连, 逻辑型, 公开, 
.参数 线程索引, 整数型, , 
.局部变量 已连接秒, 整数型, , , 

.如果真 (等于 (PT_取连接状态 (线程索引), 假))
    .如果真 (等于 (PT_连接 (线程索引), 假))
        返回 (假)
    .如果真结束
    
.如果真结束
赋值 (已连接秒, 整除 (相减 (取启动时间 (), 全_线程参数[线程索引].PT_连接时间), 1000))
.如果真 (大于或等于 (已连接秒, 60))
    调试输出 (线程索引, “连接满60秒，主动重连”)
    PT_断开 (线程索引)
    .如果真 (等于 (PT_连接 (线程索引), 假))
        返回 (假)
    .如果真结束
    
.如果真结束
返回 (真)

' ================================================================
' 从字节集读整数（小端，写到内存方式）
' ================================================================

.子程序 PT_读整数, 长整数型, 公开, 
.参数 数据源, 字节集, , 
.参数 偏移, 整数型, , 
.参数 字节数, 整数型, , 
.局部变量 子数据, 字节集, , , 
.局部变量 结果, 长整数型, , , 
.局部变量 指针, 整数型, , , 

赋值 (子数据, 取字节集中间 (数据源, 相加 (偏移, 1), 字节数))
赋值 (结果, 0)
赋值 (指针, 取变量数据地址 (结果))
写到内存 (子数据, 指针, 字节数)
返回 (结果)

' ================================================================
' 从字节集读小数（float 4字节小端）
' ================================================================

.子程序 PT_读小数, 小数型, 公开, 
.参数 数据源, 字节集, , 
.参数 偏移, 整数型, , 
.局部变量 子数据, 字节集, , , 
.局部变量 结果, 小数型, , , 
.局部变量 指针, 整数型, , , 

赋值 (子数据, 取字节集中间 (数据源, 相加 (偏移, 1), 4))
赋值 (结果, 0)
赋值 (指针, 取变量数据地址 (结果))
写到内存 (子数据, 指针, 4)
返回 (结果)

' ================================================================
' 向字节集写整数（小端，左边+值+右边）
' ================================================================

.子程序 PT_写整数, , 公开, 
.参数 目标, 字节集, 参考, 
.参数 偏移, 整数型, , 
.参数 值, 长整数型, , 
.参数 字节数, 整数型, , 
.局部变量 指针, 整数型, , , 
.局部变量 值字节集, 字节集, , , 
.局部变量 左边部分, 字节集, , , 
.局部变量 右边部分, 字节集, , , 
.局部变量 总长度, 整数型, , , 

赋值 (指针, 取变量数据地址 (值))
赋值 (值字节集, 取字节集中间 (指针到字节集 (指针, 8), 1, 字节数))
赋值 (总长度, 取字节集长度 (目标))
赋值 (左边部分, 取字节集左边 (目标, 偏移))
赋值 (右边部分, 取字节集中间 (目标, 相加 (偏移, 字节数, 1), 相减 (总长度, 偏移, 字节数)))
赋值 (目标, 相加 (左边部分, 值字节集, 右边部分))

' ================================================================
' 向字节集写小数（float 4字节小端）
' ================================================================

.子程序 PT_写小数, , 公开, 
.参数 目标, 字节集, 参考, 
.参数 偏移, 整数型, , 
.参数 值, 小数型, , 
.局部变量 指针, 整数型, , , 
.局部变量 值字节集, 字节集, , , 
.局部变量 左边部分, 字节集, , , 
.局部变量 右边部分, 字节集, , , 
.局部变量 总长度, 整数型, , , 

赋值 (指针, 取变量数据地址 (值))
赋值 (值字节集, 指针到字节集 (指针, 4))
赋值 (总长度, 取字节集长度 (目标))
赋值 (左边部分, 取字节集左边 (目标, 偏移))
赋值 (右边部分, 取字节集中间 (目标, 相加 (偏移, 4, 1), 相减 (总长度, 偏移, 4)))
赋值 (目标, 相加 (左边部分, 值字节集, 右边部分))

' ================================================================
' 发送帧（PACKET40 + FRAME24 + CONTROL32 + BGR）
' ================================================================

.子程序 PT_发送帧, 逻辑型, 公开, 
.参数 线程索引, 整数型, , 
.参数 BGR数据, 字节集, , 
.参数 宽, 整数型, , 
.参数 高, 整数型, , 
.参数 是否重置, 逻辑型, 可空, 
.局部变量 包头, 字节集, , , 
.局部变量 发送数据, 字节集, , , 
.局部变量 已发送, 整数型, , , 
.局部变量 本次发送, 整数型, , , 
.局部变量 剩余长度, 整数型, , , 
.局部变量 跟踪类型, 整数型, , , 
.局部变量 时间戳, 长整数型, , , 
.局部变量 数据长度, 整数型, , , 
.局部变量 步长, 整数型, , , 
.局部变量 发送开始, 整数型, , ,
.局部变量 套接字, 整数型, , ,

赋值 (跟踪类型, 全_线程参数[线程索引].PT_跟踪类型)
赋值 (全_线程参数[线程索引].PT_序列号, 相加 (全_线程参数[线程索引].PT_序列号, 1))
赋值 (时间戳, 取启动时间 ())
赋值 (数据长度, 取字节集长度 (BGR数据))
' 构造96字节包头
赋值 (步长, 相加 (相乘 (宽, 3), 3))
赋值 (步长, 整除 (步长, 4))
赋值 (步长, 相乘 (步长, 4))
赋值 (包头, 到字节集 (1263686736))  ' magic = 0x4B525450，ASCII 为 PTRK
赋值 (包头, 相加 (包头, 到字节集 (到短整数 (2))))  ' version = 2
赋值 (包头, 相加 (包头, 到字节集 (到短整数 (1))))  ' type = 1 (Frame)
赋值 (包头, 相加 (包头, 到字节集 (96)))  ' headerSize = 40 + 24 + 32
赋值 (包头, 相加 (包头, 到字节集 (数据长度)))  ' payloadSize
' 调试输出 (线程索引,包头1”, 包头)
赋值 (包头, 相加 (包头, 到字节集 (全_线程参数[线程索引].PT_序列号)))  ' sequence (uint64，结果原样返回)
' 调试输出 (线程索引,包头2”, 包头)
赋值 (包头, 相加 (包头, 到字节集 (时间戳)))  ' timestampMs (uint64)
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' flags（包级预留）
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' reserved
' ---------- FrameHeader (24 字节) ----------
赋值 (包头, 相加 (包头, 到字节集 (宽)))
赋值 (包头, 相加 (包头, 到字节集 (高)))
赋值 (包头, 相加 (包头, 到字节集 (步长)))  ' 每行字节数（含行尾填充）
赋值 (包头, 相加 (包头, 到字节集 (1)))  ' pixelFormat = 1 (BGR24)
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' encoding = 0 (raw)
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' reserved
' ---------- FrameControlHeader (32 字节) ----------
赋值 (包头, 相加 (包头, 到字节集 (2)))  ' trackerType = 2 (Mouse)；1 = Mushroom
赋值 (包头, 相加 (包头, 到字节集 (全_线程参数[线程索引].PT_ROI左)))  ' screenLeft：ROI 左上角屏幕绝对 X
赋值 (包头, 相加 (包头, 到字节集 (全_线程参数[线程索引].PT_ROI上)))  ' screenTop：ROI 左上角屏幕绝对 Y
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' classId（Mouse 忽略，填 0）
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' identityCount（Mouse 填 0）
赋值 (包头, 相加 (包头, 到字节集 (选择 (是否重置, 1, 0))))  ' flags：bit0(1) = 重置本连接跟踪状态
赋值 (包头, 相加 (包头, 到字节集 (2)))  ' modelType = 2 (Huashu)；1 = Mogu
赋值 (包头, 相加 (包头, 到字节集 (0)))  ' reserved1
' 调试输出 (线程索引,PT发送 seq=”, 全_线程参数 [线程索引].PT_序列号, “宽=”, 宽, “高=”, 高, “BGR=”, 数据长度, “tracker=”, 跟踪类型, “reset=”, 到文本 (是否重置))
赋值 (发送数据, 相加 (包头, BGR数据))
赋值 (套接字, PT_取套接字 (线程索引))
.如果真 (等于 (套接字, 0))
    赋值 (全_线程参数[线程索引].PT_最后错误, “发送时连接已关闭”)
    赋值 (发送数据, {})
    返回 (假)
.如果真结束
赋值 (已发送, 0)
赋值 (剩余长度, 取字节集长度 (发送数据))
赋值 (发送开始, 取启动时间 ())
.判断循环首 (小于 (已发送, 剩余长度))
    .如果真 (或者 (PT_应取消 (线程索引), 大于或等于 (相减 (取启动时间 (), 发送开始), 2000)))
        赋值 (全_线程参数[线程索引].PT_最后错误, “发送已取消或超时”)
        赋值 (发送数据, {})
        PT_断开 (线程索引)
        返回 (假)
    .如果真结束
    赋值 (本次发送, send_指针 (套接字, 相加 (取变量数据地址 (发送数据), 已发送), 相减 (剩余长度, 已发送), 0))
    .如果真 (小于或等于 (本次发送, 0))
        赋值 (全_线程参数[线程索引].PT_最后错误, “发送失败”)
        调试工具 (线程索引, 相加 (“PT 发送帧Error:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
        赋值 (发送数据, {})
        PT_断开 (线程索引)
        返回 (假)
    .如果真结束
    赋值 (已发送, 相加 (已发送, 本次发送))
.判断循环尾 ()
赋值 (发送数据, {})
返回 (真)

' ================================================================
' 精确接收指定字节数（处理TCP拆包）
' ================================================================

.子程序 PT_接收指定字节数, 逻辑型, 公开, 
.参数 线程索引, 整数型, , 
.参数 接收缓冲区, 字节集, 参考, 
.参数 期望字节数, 整数型, , 
.局部变量 已接收, 整数型, , , 
.局部变量 本次接收, 整数型, , , 
.局部变量 临时缓冲, 字节集, , , 
.局部变量 结果, 字节集, , , 
.局部变量 接收开始, 整数型, , ,
.局部变量 套接字, 整数型, , ,

.如果真 (或者 (小于 (期望字节数, 0), 大于 (期望字节数, 1048576)))
    赋值 (全_线程参数[线程索引].PT_最后错误, “接收长度越界”)
    PT_断开 (线程索引)
    返回 (假)
.如果真结束
赋值 (结果, 取空白字节集 (0))
赋值 (套接字, PT_取套接字 (线程索引))
.如果真 (等于 (套接字, 0))
    赋值 (全_线程参数[线程索引].PT_最后错误, “接收时连接已关闭”)
    返回 (假)
.如果真结束
赋值 (已接收, 0)
赋值 (接收开始, 取启动时间 ())
.判断循环首 (小于 (已接收, 期望字节数))
    .如果真 (或者 (PT_应取消 (线程索引), 大于或等于 (相减 (取启动时间 (), 接收开始), 2000)))
        赋值 (全_线程参数[线程索引].PT_最后错误, “接收已取消或超时”)
        PT_断开 (线程索引)
        返回 (假)
    .如果真结束
    赋值 (临时缓冲, 取空白字节集 (相减 (期望字节数, 已接收)))
    赋值 (本次接收, recv (套接字, 临时缓冲, 相减 (期望字节数, 已接收), 0))
    .如果真 (小于或等于 (本次接收, 0))
        赋值 (全_线程参数[线程索引].PT_最后错误, “接收失败/连接关闭”)
        PT_断开 (线程索引)
        调试工具 (线程索引, 相加 (“PT 接收Error:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
        返回 (假)
    .如果真结束
    赋值 (结果, 相加 (结果, 取字节集中间 (临时缓冲, 1, 本次接收)))
    赋值 (已接收, 相加 (已接收, 本次接收))
.判断循环尾 ()
赋值 (接收缓冲区, 结果)
返回 (真)

' ================================================================
' 接收响应
' 返回: 1=成功有结果, 2=Error, 0=失败/无结果
' ================================================================

.子程序 PT_接收响应, 整数型, 公开, 
.参数 线程索引, 整数型, , 
.局部变量 响应头, 字节集, , , 
.局部变量 magic, 长整数型, , , 
.局部变量 响应类型, 整数型, , , 
.局部变量 包头大小, 整数型, , , 
.局部变量 载荷大小, 长整数型, , , 
.局部变量 序列号, 长整数型, , , 
.局部变量 扩展头, 字节集, , , 
.局部变量 扩展大小, 整数型, , , 
.局部变量 跟踪数量, 整数型, , , 
.局部变量 推理耗时, 小数型, , , 
.局部变量 跟踪耗时, 小数型, , , 
.局部变量 响应体, 字节集, , , 
.局部变量 偏移, 整数型, , , 
.局部变量 结果, PT_TrackResult, , , 
.局部变量 错误消息, 字节集, , , 
.局部变量 错误文本, 文本型, , , 
.局部变量 期望载荷, 整数型, , ,

.如果真 (等于 (PT_接收指定字节数 (线程索引, 响应头, 40), 假))
    返回 (0)
.如果真结束
赋值 (magic, PT_读整数 (响应头, 0, 4))
赋值 (响应类型, PT_读整数 (响应头, 6, 2))
赋值 (包头大小, PT_读整数 (响应头, 8, 4))
赋值 (载荷大小, PT_读整数 (响应头, 12, 4))
赋值 (序列号, PT_读整数 (响应头, 16, 8))
.如果真 (不等于 (magic, 进制_十六到十 (“4B525450”)))
    赋值 (全_线程参数[线程索引].PT_最后错误, “响应magic错误”)
    调试工具 (线程索引, 相加 (“PT Error0:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
    调试输出 (线程索引, 相加 (“PT Error0:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
.如果真 (或者 (小于 (包头大小, 40), 大于 (包头大小, 4096), 小于 (载荷大小, 0), 大于 (载荷大小, 1048576)))
    赋值 (全_线程参数[线程索引].PT_最后错误, “响应长度越界”)
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
赋值 (扩展大小, 相减 (包头大小, 40))
赋值 (扩展头, 取空白字节集 (0))
' DM ＝ 全_线程参数 [线程索引].DM
.如果真 (大于 (扩展大小, 0))
    .如果真 (等于 (PT_接收指定字节数 (线程索引, 扩展头, 扩展大小), 假))
        返回 (0)
    .如果真结束
    
.如果真结束
.如果真 (等于 (响应类型, 3))
    .如果真 (大于 (载荷大小, 8192))
        赋值 (全_线程参数[线程索引].PT_最后错误, “错误响应载荷过大”)
        PT_断开 (线程索引)
        返回 (0)
    .如果真结束
    .如果真 (大于 (载荷大小, 0))
        .如果真 (等于 (PT_接收指定字节数 (线程索引, 错误消息, 载荷大小), 假))
            返回 (0)
        .如果真结束
    .如果真结束
    赋值 (错误文本, 到文本 (错误消息))
    赋值 (全_线程参数[线程索引].PT_最后错误, 相加 (“服务端Error:”, 错误文本))
    赋值 (全_线程参数[线程索引].PT_有结果, 假)
    调试输出 (线程索引, “PT Error:”, 错误文本)
    调试工具 (线程索引, 相加 (“PT Error丢失:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
    PT_断开 (线程索引)
    返回 (2)
.如果真结束
.如果真 (不等于 (响应类型, 2))
    赋值 (全_线程参数[线程索引].PT_最后错误, 相加 (“未知响应类型:”, 到文本 (响应类型)))
    调试工具 (线程索引, 相加 (“PT Error2:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
    调试输出 (线程索引, 相加 (“PT Error2:”, 到文本 (全_线程参数[线程索引].PT_最后错误)))
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
赋值 (全_线程参数[线程索引].PT_有结果, 假)
.如果真 (小于 (取字节集长度 (扩展头), 16))
    赋值 (全_线程参数[线程索引].PT_最后错误, “跟踪响应扩展头过短”)
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
赋值 (跟踪数量, PT_读整数 (扩展头, 0, 4))
赋值 (推理耗时, PT_读小数 (扩展头, 4))
赋值 (跟踪耗时, PT_读小数 (扩展头, 8))
.如果真 (或者 (小于 (跟踪数量, 0), 大于 (跟踪数量, 256)))
    赋值 (全_线程参数[线程索引].PT_最后错误, “跟踪数量越界”)
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
赋值 (期望载荷, 相乘 (跟踪数量, 32))
.如果真 (不等于 (载荷大小, 期望载荷))
    赋值 (全_线程参数[线程索引].PT_最后错误, “跟踪响应载荷长度不匹配”)
    PT_断开 (线程索引)
    返回 (0)
.如果真结束
.如果真 (等于 (跟踪数量, 0))
    返回 (1)
.如果真结束
.如果真 (等于 (PT_接收指定字节数 (线程索引, 响应体, 相乘 (跟踪数量, 32)), 假))
    返回 (0)
.如果真结束
赋值 (偏移, 0)
赋值 (结果.trackId, PT_读整数 (响应体, 偏移, 4))
赋值 (结果.classId, PT_读整数 (响应体, 相加 (偏移, 4), 4))
赋值 (结果.confidence, PT_读小数 (响应体, 相加 (偏移, 8)))
赋值 (结果.x, PT_读小数 (响应体, 相加 (偏移, 12)))
赋值 (结果.y, PT_读小数 (响应体, 相加 (偏移, 16)))
赋值 (结果.w, PT_读小数 (响应体, 相加 (偏移, 20)))
赋值 (结果.h, PT_读小数 (响应体, 相加 (偏移, 24)))
赋值 (结果.state, PT_读整数 (响应体, 相加 (偏移, 28), 4))
赋值 (全_线程参数[线程索引].PT_最后结果, 结果)
赋值 (全_线程参数[线程索引].PT_有结果, 真)
' 调试工具 (线程索引, 相加 (“PT Track 坐标=”, 到文本 (结果.x), “,”, 到文本 (结果.y)))
' 调试输出 (线程索引, “PT Track x=”, 结果.x, “y=”, 结果.y, “state=”, 结果.state, “conf=”, 结果.confidence, “trackId=”, 结果.trackId)
VNC_鼠标_移动 (线程索引, 取整 (结果.x), 取整 (结果.y))
' 全局DM.MoveTo (取整 (结果.x), 取整 (结果.y))
返回 (1)

' ================================================================
' 字节集辅助
' ================================================================

.子程序 字节集_到整数, 字节集, 公开, 
.参数 值, 整数型, , 
.局部变量 指针, 整数型, , , 
.局部变量 结果, 字节集, , , 

赋值 (结果, 取空白字节集 (4))
赋值 (指针, 取变量数据地址 (值))
写到内存 (结果, 指针, 4)
返回 (结果)

.子程序 字节集_到短整数, 字节集, 公开, 
.参数 值, 短整数型, , 
.局部变量 指针, 整数型, , , 
.局部变量 结果, 字节集, , , 

赋值 (结果, 取空白字节集 (2))
赋值 (指针, 取变量数据地址 (值))
写到内存 (结果, 指针, 2)
返回 (结果)

' ================================================================
' DM截图（GetScreenDataBmp）→ 提取BGR24
' ================================================================

.子程序 获取截图数据Bmp3, 逻辑型, 公开, 
.参数 线程索引, 整数型, , 
.参数 x1, 整数型, , 
.参数 y1, 整数型, , 
.参数 x2, 整数型, , 
.参数 y2, 整数型, , 
.参数 BGR数据, 字节集, 参考, 
.参数 宽, 整数型, , 
.参数 高, 整数型, , 
.局部变量 数据指针, 整数型, , , 
.局部变量 数据大小, 整数型, , , 
.局部变量 步长, 整数型, , , 
.局部变量 BMP数据, 字节集, , , 
.局部变量 结果, 字节集, , , 
.局部变量 行, 整数型, , , 
.局部变量 DM, dmsoft, , , 
.局部变量 开始, 整数型, , , 
.局部变量 源地址, 整数型, , , 
.局部变量 目标地址, 整数型, , , 
.局部变量 BMP首地址, 整数型, , , 
.局部变量 结果首地址, 整数型, , , 
.局部变量 需要大小, 整数型, , ,

赋值 (DM, 全_线程参数[线程索引].DM)
.如果真 (或者 (小于或等于 (宽, 0), 小于或等于 (高, 0), 大于 (宽, 4096), 大于 (高, 4096)))
    赋值 (全_线程参数[线程索引].PT_最后错误, “截图尺寸越界”)
    返回 (假)
.如果真结束
赋值 (步长, 相加 (相乘 (宽, 3), 3))
赋值 (步长, 整除 (步长, 4))
赋值 (步长, 相乘 (步长, 4))
赋值 (需要大小, 相加 (54, 相乘 (步长, 高)))
赋值 (开始, 取启动时间 ())
线程_进入许可区 (全_线程参数[线程索引].图色许可)
DM.GetScreenDataBmp (x1, y1, x2, y2, 数据指针, 数据大小)
.如果真 (或者 (等于 (数据指针, 0), 小于 (数据大小, 需要大小)))
    线程_退出许可区 (全_线程参数[线程索引].图色许可)
    赋值 (全_线程参数[线程索引].PT_最后错误, “BMP数据长度不足”)
    返回 (假)
.如果真结束
' DM 返回的是内部指针，必须在同一许可区内复制完成
赋值 (BMP数据, 指针到字节集 (数据指针, 数据大小))
线程_退出许可区 (全_线程参数[线程索引].图色许可)

' 调试工具 (线程索引, “截图耗时:” ＋ 到文本 (取启动时间 () － 开始))
赋值 (开始, 取启动时间 ())
赋值 (BMP首地址, 取变量数据地址 (BMP数据))
' 预分配结果（和函数2一样，避免 O(n2) 拼接）
赋值 (结果, 取空白字节集 (相乘 (步长, 高)))
赋值 (结果首地址, 取变量数据地址 (结果))
' 逐行 memcpy：源是 BMP字节集的地址，目标是结果字节集的地址
.变量循环首 (1, 高, 1, 行)
    赋值 (源地址, 相加 (BMP首地址, 54, 相乘 (相减 (高, 行), 步长)))
    赋值 (目标地址, 相加 (结果首地址, 相乘 (相减 (行, 1), 步长)))
    memcpy (目标地址, 源地址, 步长)
.变量循环尾 ()
赋值 (BGR数据, 结果)
' 调试工具 (线程索引, “转码耗时:” ＋ 到文本 (取启动时间 () － 开始))
' 调试输出 (取启动时间 () － 开始)
返回 (真)

.子程序 获取截图数据Bmp, 逻辑型, 公开, 
.参数 线程索引, 整数型, , 
.参数 x1, 整数型, , 
.参数 y1, 整数型, , 
.参数 x2, 整数型, , 
.参数 y2, 整数型, , 
.参数 BGR数据, 字节集, 可空, 
.参数 宽, 整数型, 可空, 
.参数 高, 整数型, 可空, 
.局部变量 数据指针, 整数型, , , 
.局部变量 数据大小, 整数型, , , 
.局部变量 步长, 整数型, , , 
.局部变量 行, 整数型, , , 
.局部变量 DM, dmsoft, , , 
.局部变量 开始, 整数型, , , 
.局部变量 源地址, 整数型, , , 
.局部变量 目标地址, 整数型, , , 
.局部变量 BMP像素首, 整数型, , , 
.局部变量 BGR首地址, 整数型, , , 

赋值 (DM, 全_线程参数[线程索引].DM)

' 24位 BMP：源步长 = 宽 × 3，目标也是 24位 BGR = 宽 × 3
赋值 (步长, 相乘 (宽, 3))

赋值 (开始, 取启动时间 ())
线程_进入许可区 (全_线程参数[线程索引].图色许可)
DM.GetScreenDataBmp (x1, y1, x2, y2, 数据指针, 数据大小)
调试工具 (线程索引, 相加 (“截图耗时:”, 到文本 (相减 (取启动时间 (), 开始))))

.如果真 (或者 (等于 (数据指针, 0), 小于 (数据大小, 54)))
    线程_退出许可区 (全_线程参数[线程索引].图色许可)
    赋值 (全_线程参数[线程索引].PT_最后错误, “大漠截图返回空”)
    返回 (假)
.如果真结束

赋值 (开始, 取启动时间 ())

' 像素数据从偏移 54 开始
赋值 (BMP像素首, 相加 (数据指针, 54))

' 分配目标 BGR24（顶部向下）
赋值 (BGR数据, 取空白字节集 (相乘 (步长, 高)))
赋值 (BGR首地址, 取变量数据地址 (BGR数据))

' 垂直翻转：BMP自底向上 → 目标顶部向下，整行 memcpy
.变量循环首 (1, 高, 1, 行)
    赋值 (源地址, 相加 (BMP像素首, 相乘 (相减 (高, 行), 步长)))
    赋值 (目标地址, 相加 (BGR首地址, 相乘 (相减 (行, 1), 步长)))
    memcpy (目标地址, 源地址, 步长)
.变量循环尾 ()
线程_退出许可区 (全_线程参数[线程索引].图色许可)

调试工具 (线程索引, 相加 (“转码耗时:”, 到文本 (相减 (取启动时间 (), 开始))))
返回 (真)

.子程序 PT_跟踪一次, 整数型, 公开, 
.参数 线程索引, 整数型, , 
.参数 是否重置, 逻辑型, 可空, 
.局部变量 BGR数据, 字节集, , , 
.局部变量 接收结果, 整数型, , , 
.局部变量 宽度, 整数型, , , 
.局部变量 高度, 整数型, , , 
.局部变量 开始, 整数型, , , 
.局部变量 耗时, 整数型, , , 
.局部变量 检查重连耗时, 整数型, , , 
.局部变量 截图数据耗时, 整数型, , , 
.局部变量 发送帧耗时, 整数型, , , 
.局部变量 总开始, 整数型, , , 
.局部变量 PT_接收响应耗时, 整数型, , , 
.局部变量 总耗时, 整数型, , , 

.如果真 (PT_应取消 (线程索引))
    返回 (0)
.如果真结束
赋值 (宽度, 相减 (全_线程参数[线程索引].PT_ROI右, 全_线程参数[线程索引].PT_ROI左))
赋值 (高度, 相减 (全_线程参数[线程索引].PT_ROI下, 全_线程参数[线程索引].PT_ROI上))
赋值 (总开始, 取启动时间 ())
.如果真 (等于 (PT_检查重连 (线程索引), 假))
    返回 (0)
.如果真结束
.如果真 (PT_应取消 (线程索引))
    返回 (0)
.如果真结束
赋值 (耗时, 相减 (取启动时间 (), 总开始))
赋值 (检查重连耗时, 耗时)
赋值 (开始, 取启动时间 ())
.如果真 (等于 (获取截图数据Bmp3 (线程索引, 全_线程参数[线程索引].PT_ROI左, 全_线程参数[线程索引].PT_ROI上, 全_线程参数[线程索引].PT_ROI右, 全_线程参数[线程索引].PT_ROI下, BGR数据, 宽度, 高度), 假))
    返回 (0)
.如果真结束
.如果真 (PT_应取消 (线程索引))
    赋值 (BGR数据, {})
    返回 (0)
.如果真结束
赋值 (耗时, 相减 (取启动时间 (), 开始))
赋值 (截图数据耗时, 耗时)
赋值 (开始, 取启动时间 ())
赋值 (全_线程参数[线程索引].截图次数, 相加 (全_线程参数[线程索引].截图次数, 1))
.如果真 (等于 (PT_发送帧 (线程索引, BGR数据, 宽度, 高度, 是否重置), 假))
    返回 (0)
.如果真结束
赋值 (耗时, 相减 (取启动时间 (), 开始))
赋值 (发送帧耗时, 耗时)
赋值 (开始, 取启动时间 ())
赋值 (接收结果, PT_接收响应 (线程索引))
赋值 (耗时, 相减 (取启动时间 (), 开始))
赋值 (PT_接收响应耗时, 耗时)
赋值 (BGR数据, {})
赋值 (总耗时, 相减 (取启动时间 (), 总开始))
' 调试工具 (线程索引, 相加 (“总耗时”, 到文本 (总耗时), “  检查重连耗时”, 到文本 (检查重连耗时), “  截图数据耗时”, 到文本 (截图数据耗时), “  发送帧耗时”, 到文本 (发送帧耗时), “  PT_接收响应耗时”, 到文本 (PT_接收响应耗时)))
.如果真 (等于 (接收结果, 2))
    返回 (3)
.如果真结束
.如果真 (不等于 (接收结果, 1))
    返回 (0)
.如果真结束
.如果真 (并且 (等于 (全_线程参数[线程索引].PT_有结果, 真), 等于 (全_线程参数[线程索引].PT_最后结果.state, 1)))
    返回 (1)
.如果真结束
返回 (2)


