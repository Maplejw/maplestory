.程序集 VNC_操作, , , 

.子程序 VNC_认证, 整数型, , 
.参数 pSender, 整数型, , 
.参数 dwConnID, 整数型, , 
.参数 pData, 整数型, , 数据指针 需通过指针到字节集获取数据
.参数 iLength, 整数型, , 缓冲区长度
.局部变量 data, 字节集, , , 
.局部变量 线程索引, 整数型, , , 
.局部变量 桌面宽度, 短整数型, , , 
.局部变量 桌面高度, 短整数型, , , 
.局部变量 缓存长度, 整数型, , , 
.局部变量 消费长度, 整数型, , , 
.局部变量 类型数量, 整数型, , , 
.局部变量 安全类型, 整数型, , , 
.局部变量 名称长度, 整数型, , , 
.局部变量 i, 整数型, , , 
.局部变量 响应数据, 字节集, , , 
.局部变量 可以继续, 逻辑型, , , 
.局部变量 完成握手, 逻辑型, , , 

赋值 (线程索引, 辅助_ID取序号 (pSender))
.如果真 (或者 (小于 (线程索引, 1), 大于 (线程索引, 取数组成员数 (全_线程参数))))
    ' 旧连接销毁后的迟到回调，不能再访问线程数组
    返回 (0)
.如果真结束
.如果真 (或者 (小于或等于 (pData, 0), 小于或等于 (iLength, 0)))
    返回 (0)
.如果真结束
赋值 (data, 指针_到字节集 (pData, iLength))
线程_进入许可区 (全_线程参数[线程索引].VNC许可)
.如果真 (或者 (不等于 (全_线程参数[线程索引].id, pSender), 小于 (全_线程参数[线程索引].组包位置, 0), 大于或等于 (全_线程参数[线程索引].组包位置, 4)))
    线程_退出许可区 (全_线程参数[线程索引].VNC许可)
    返回 (0)
.如果真结束
赋值 (全_线程参数[线程索引].组包缓存, 相加 (全_线程参数[线程索引].组包缓存, data))
.如果真 (大于 (取字节集长度 (全_线程参数[线程索引].组包缓存), 1048576))
    赋值 (全_线程参数[线程索引].组包缓存, {})
    赋值 (全_线程参数[线程索引].组包位置, -1)
    赋值 (全_线程参数[线程索引].VNC连接就绪, 假)
    赋值 (全_线程参数[线程索引].VNC断线标志, 真)
.如果真结束
线程_退出许可区 (全_线程参数[线程索引].VNC许可)

' TCP可能拆包或把多个握手阶段粘在一起，循环只消费已经完整到达的阶段
.判断循环首 (真)
    赋值 (响应数据, {})
    赋值 (消费长度, 0)
    赋值 (可以继续, 假)
    赋值 (完成握手, 假)
    线程_进入许可区 (全_线程参数[线程索引].VNC许可)
    .如果真 (或者 (不等于 (全_线程参数[线程索引].id, pSender), 小于 (全_线程参数[线程索引].组包位置, 0), 大于或等于 (全_线程参数[线程索引].组包位置, 4)))
        线程_退出许可区 (全_线程参数[线程索引].VNC许可)
        跳出循环 ()
    .如果真结束
    赋值 (缓存长度, 取字节集长度 (全_线程参数[线程索引].组包缓存))
    .判断开始 (等于 (全_线程参数[线程索引].组包位置, 0))
        .如果真 (大于或等于 (缓存长度, 12))
            赋值 (消费长度, 12)
            .如果 (等于 (取字节集左边 (全_线程参数[线程索引].组包缓存, 12), {82, 70, 66, 32, 48, 48, 51, 46, 48, 48, 56, 10}))
                赋值 (响应数据, {82, 70, 66, 32, 48, 48, 51, 46, 48, 48, 56, 10})
                赋值 (全_线程参数[线程索引].组包位置, 1)
                赋值 (可以继续, 真)
            .否则
                赋值 (全_线程参数[线程索引].组包位置, -1)
                赋值 (全_线程参数[线程索引].VNC断线标志, 真)
            .如果结束
        .如果真结束
    .判断 (等于 (全_线程参数[线程索引].组包位置, 1))
        .如果真 (大于或等于 (缓存长度, 1))
            赋值 (类型数量, 取字节集数据 (全_线程参数[线程索引].组包缓存, #字节型, 1))
            .如果 (小于或等于 (类型数量, 0))
                赋值 (全_线程参数[线程索引].组包位置, -1)
                赋值 (全_线程参数[线程索引].VNC断线标志, 真)
            .否则
                .如果真 (大于或等于 (缓存长度, 相加 (类型数量, 1)))
                    赋值 (安全类型, 0)
                    .计次循环首 (类型数量, i)
                        .如果真 (等于 (取字节集数据 (全_线程参数[线程索引].组包缓存, #字节型, 相加 (i, 1)), 1))
                            赋值 (安全类型, 1)
                            跳出循环 ()
                        .如果真结束
                    .计次循环尾 ()
                    赋值 (消费长度, 相加 (类型数量, 1))
                    .如果 (等于 (安全类型, 1))
                        赋值 (响应数据, {1})
                        赋值 (全_线程参数[线程索引].组包位置, 2)
                        赋值 (可以继续, 真)
                    .否则
                        赋值 (全_线程参数[线程索引].组包位置, -1)
                        赋值 (全_线程参数[线程索引].VNC断线标志, 真)
                    .如果结束
                .如果真结束
            .如果结束
        .如果真结束
    .判断 (等于 (全_线程参数[线程索引].组包位置, 2))
        .如果真 (大于或等于 (缓存长度, 4))
            赋值 (消费长度, 4)
            .如果 (等于 (取字节集左边 (全_线程参数[线程索引].组包缓存, 4), {0, 0, 0, 0}))
                赋值 (响应数据, {0})
                赋值 (全_线程参数[线程索引].组包位置, 3)
                赋值 (可以继续, 真)
            .否则
                赋值 (全_线程参数[线程索引].组包位置, -1)
                赋值 (全_线程参数[线程索引].VNC断线标志, 真)
            .如果结束
        .如果真结束
    .判断 (等于 (全_线程参数[线程索引].组包位置, 3))
        .如果真 (大于或等于 (缓存长度, 24))
            赋值 (名称长度, htonl (取字节集数据 (全_线程参数[线程索引].组包缓存, #整数型, 21)))
            .如果 (或者 (小于 (名称长度, 0), 大于 (名称长度, 1048576)))
                赋值 (全_线程参数[线程索引].组包位置, -1)
                赋值 (全_线程参数[线程索引].VNC断线标志, 真)
            .否则
                .如果真 (大于或等于 (缓存长度, 相加 (24, 名称长度)))
                    赋值 (桌面宽度, htons (取字节集数据 (全_线程参数[线程索引].组包缓存, #短整数型, 1)))
                    赋值 (桌面高度, htons (取字节集数据 (全_线程参数[线程索引].组包缓存, #短整数型, 3)))
                    赋值 (消费长度, 相加 (24, 名称长度))
                    赋值 (全_线程参数[线程索引].组包位置, 4)
                    赋值 (全_线程参数[线程索引].VNC连接就绪, 真)
                    赋值 (可以继续, 真)
                    赋值 (完成握手, 真)
                .如果真结束
            .如果结束
        .如果真结束
    .判断结束
    .如果真 (大于 (消费长度, 0))
        .如果 (大于 (缓存长度, 消费长度))
            赋值 (全_线程参数[线程索引].组包缓存, 取字节集右边 (全_线程参数[线程索引].组包缓存, 相减 (缓存长度, 消费长度)))
        .否则
            赋值 (全_线程参数[线程索引].组包缓存, {})
        .如果结束
    .如果真结束
    线程_退出许可区 (全_线程参数[线程索引].VNC许可)
    .如果真 (大于 (取字节集长度 (响应数据), 0))
        .如果真 (等于 (VNC_握手发送 (线程索引, pSender, 响应数据), 假))
            跳出循环 ()
        .如果真结束
    .如果真结束
    .如果真 (完成握手)
        调试输出 (线程索引, 桌面宽度, 桌面高度)
        VNC_键盘_全部弹起 (线程索引)
        VNC_鼠标_全部弹起 (线程索引)
        线程_进入许可区 (全_线程参数[线程索引].VNC许可)
        .如果真 (并且 (等于 (全_线程参数[线程索引].id, pSender), 全_线程参数[线程索引].VNC连接就绪))
            赋值 (全_线程参数[线程索引].VNC断线标志, 假)
        .如果真结束
        线程_退出许可区 (全_线程参数[线程索引].VNC许可)
        跳出循环 ()
    .如果真结束
    .如果真 (等于 (可以继续, 假))
        跳出循环 ()
    .如果真结束
.判断循环尾 ()

返回 (0)

.子程序 VNC_握手发送, 逻辑型, , 握手回调发送也与连接销毁互斥
.参数 线程索引, 整数型, , 
.参数 连接ID, 整数型, , 
.参数 数据, 字节集, , 
.局部变量 发送结果, 逻辑型, , , 

线程_进入许可区 (全_线程参数[线程索引].VNC许可)
.如果真 (或者 (小于或等于 (连接ID, 0), 不等于 (全_线程参数[线程索引].id, 连接ID)))
    线程_退出许可区 (全_线程参数[线程索引].VNC许可)
    返回 (假)
.如果真结束
赋值 (发送结果, 全_线程参数[线程索引].m_Client.发送字节集 (连接ID, 数据))
.如果真 (等于 (发送结果, 假))
    赋值 (全_线程参数[线程索引].VNC连接就绪, 假)
    赋值 (全_线程参数[线程索引].VNC断线标志, 真)
    赋值 (全_线程参数[线程索引].组包缓存, {})
    赋值 (全_线程参数[线程索引].组包位置, -1)
.如果真结束
线程_退出许可区 (全_线程参数[线程索引].VNC许可)
返回 (发送结果)

.子程序 VNC_连接断开, 整数型, , HPSocket 断开回调
.参数 pSender, 整数型, , 
.参数 dwConnID, 整数型, , 
.参数 enOperation, 整数型, , 
.参数 iErrorCode, 整数型, , 
.局部变量 线程索引, 整数型, , , 

赋值 (线程索引, 辅助_ID取序号 (pSender))
.如果真 (并且 (大于或等于 (线程索引, 1), 小于或等于 (线程索引, 取数组成员数 (全_线程参数))))
    线程_进入许可区 (全_线程参数[线程索引].VNC许可)
    .如果真 (等于 (全_线程参数[线程索引].id, pSender))
        赋值 (全_线程参数[线程索引].VNC连接就绪, 假)
        赋值 (全_线程参数[线程索引].VNC断线标志, 真)
        赋值 (全_线程参数[线程索引].组包缓存, {})
        赋值 (全_线程参数[线程索引].组包位置, -1)
    .如果真结束
    线程_退出许可区 (全_线程参数[线程索引].VNC许可)
.如果真结束
返回 (0)

.子程序 辅助_ID取序号, 整数型, , 
.参数 ID, 整数型, , 
.局部变量 i, 整数型, , , 

.如果真 (小于或等于 (ID, 0))
    返回 (0)
.如果真结束
.计次循环首 (取数组成员数 (全_线程参数), i)
    .如果真 (等于 (全_线程参数[i].id, ID))
        返回 (i)
    .如果真结束
    
.计次循环尾 ()
返回 (0)

.子程序 VNC_组合按键ctrl, , , 
.参数 线程序号, 整数型, , 
.参数 键名, 整数型, , 

VNC_键盘_按下 (线程序号, 17)

VNC_键盘_按键 (线程序号, 键名)

VNC_键盘_弹起 (线程序号, 17)

.子程序 VNC_组合按键shift, , , 
.参数 线程序号, 整数型, , 
.参数 键名, 整数型, , 

VNC_键盘_按下 (线程序号, 16)
VNC_键盘_按键 (线程序号, 键名)
VNC_键盘_弹起 (线程序号, 16)

.子程序 VNC_组合按键alt, , , 
.参数 线程序号, 整数型, , 
.参数 键名, 整数型, , 

VNC_键盘_按下 (线程序号, 18)

VNC_键盘_按键 (线程序号, 键名)

VNC_键盘_弹起 (线程序号, 18)

.子程序 VNC_文本输入, , , 
.参数 线程序号, 整数型, , 
.参数 输入文本, 文本型, , 
.局部变量 长度, 整数型, , , 
.局部变量 i, 整数型, , , 
.局部变量 返回值, 文本型, , , 
.局部变量 转换, 整数型, , , 

赋值 (长度, 取文本长度 (输入文本))
.计次循环首 (长度, i)
    赋值 (返回值, 取文本中间 (输入文本, i, 1))
    赋值 (转换, 转键代码 (线程序号, 返回值))
    .如果 (等于 (转换, 999))
        VNC_组合按键shift (线程序号, 50)
    .否则
        VNC_键盘_按键 (线程序号, 转换)
        程序_延时 (100, 0)
    .如果结束
    
.计次循环尾 ()

.子程序 VNC_鼠标, , 公开, 鼠标；新掩码=-1时保留当前按键状态
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 
.参数 新掩码, 整数型, , 
.局部变量 有效掩码, 整数型, , , 
.局部变量 发送结果, 逻辑型, , , 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 ()
.如果真结束
线程_进入许可区 (全_线程参数[序号].运行状态许可)
.如果 (大于或等于 (新掩码, 0))
    赋值 (有效掩码, 新掩码)
.否则
    赋值 (有效掩码, 全_线程参数[序号].鼠标掩码)
.如果结束
.如果真 (并且 (或者 (不等于 (全_线程参数[序号].测谎状态, 0), 等于 (全_线程参数[序号].暂停中, 1), 全_线程参数[序号].停止, 全_线程停止标志[序号], 全_请求停止所有, 全_程序退出中, 全_线程参数[序号].VNC断线标志), 不等于 (有效掩码, 0)))
    ' 非运行状态拒绝新的按下动作；掩码0的移动/弹起仍放行
    线程_退出许可区 (全_线程参数[序号].运行状态许可)
    返回 ()
.如果真结束
.如果真 (大于或等于 (新掩码, 0))
    赋值 (全_线程参数[序号].鼠标掩码, 新掩码)
.如果真结束
线程_进入许可区 (全_线程参数[序号].VNC许可)
.如果真 (或者 (小于或等于 (全_线程参数[序号].id, 0), 等于 (全_线程参数[序号].VNC连接就绪, 假)))
    赋值 (全_线程参数[序号].VNC断线标志, 真)
    线程_退出许可区 (全_线程参数[序号].VNC许可)
    线程_退出许可区 (全_线程参数[序号].运行状态许可)
    返回 ()
.如果真结束
赋值 (发送结果, 全_线程参数[序号].m_Client.发送字节集 (全_线程参数[序号].id, 相加 ({5}, 到字节集 (到字节 (位与 (有效掩码, 255))), 到字节集 (到短整数 (htons (x))), 到字节集 (到短整数 (htons (y))))))
.如果真 (等于 (发送结果, 假))
    赋值 (全_线程参数[序号].VNC连接就绪, 假)
    赋值 (全_线程参数[序号].VNC断线标志, 真)
.如果真结束
线程_退出许可区 (全_线程参数[序号].VNC许可)
线程_退出许可区 (全_线程参数[序号].运行状态许可)

.子程序 VNC_鼠标_取当前位置, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, 参考, 
.参数 y, 整数型, 参考, 

赋值 (x, 全_线程参数[序号].x)
赋值 (y, 全_线程参数[序号].y)

.子程序 VNC_键盘_按下, , 公开, 
.参数 序号, 整数型, , 
.参数 key, 整数型, , 
.局部变量 键码, 整数型, , , 
.局部变量 i, 整数型, , , 
.局部变量 已存在, 逻辑型, , , 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 ()
.如果真结束
.如果真 (或者 (小于或等于 (key, 0), 大于 (key, 256)))
    返回 ()
.如果真结束
' 状态修改与网络发送放在同一许可区，避免清键线程穿插在按下动作中间
赋值 (键码, 辅助_key取键码 (key))
.如果真 (小于或等于 (键码, 0))
    返回 ()
.如果真结束
' 记录按住状态（去重），供全部弹起使用
线程_进入许可区 (全_线程参数[序号].键状态锁)
.如果真 (或者 (不等于 (全_线程参数[序号].测谎状态, 0), 等于 (全_线程参数[序号].暂停中, 1), 全_线程参数[序号].停止, 全_线程停止标志[序号], 全_请求停止所有, 全_程序退出中, 全_线程参数[序号].VNC断线标志))
    ' 状态检查必须和清键使用同一把锁，避免清完后又插入一次按下
    线程_退出许可区 (全_线程参数[序号].键状态锁)
    返回 ()
.如果真结束
赋值 (已存在, 假)
.计次循环首 (取数组成员数 (全_线程参数[序号].按住键), i)
    .如果真 (等于 (全_线程参数[序号].按住键[i], key))
        赋值 (已存在, 真)
        跳出循环 ()
    .如果真结束
    
.计次循环尾 ()
.如果真 (等于 (已存在, 假))
    加入成员 (全_线程参数[序号].按住键, key)
.如果真结束
VNC_VNC_键盘 (序号, 1, 键码)
线程_退出许可区 (全_线程参数[序号].键状态锁)

.子程序 VNC_鼠标_滚轮上滚, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标 (序号, x, y, 8)
VNC_鼠标 (序号, x, y, 0)

.子程序 VNC_鼠标_滚轮下滚, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标 (序号, x, y, 16)
VNC_鼠标 (序号, x, y, 0)

.子程序 VNC_鼠标_右键弹起, , 公开, 
.参数 序号, 整数型, , 
.参数 x, , , 
.参数 y, , , 

VNC_鼠标 (序号, x, y, 0)

.子程序 VNC_鼠标_右键按住, , 公开, 
.参数 序号, 整数型, , 
.参数 x, , , 
.参数 y, , , 

VNC_鼠标 (序号, x, y, 4)

.子程序 VNC_鼠标_右键单击, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标_右键按住 (序号, x, y)
程序_延时 (60, )
VNC_鼠标_右键弹起 (序号, x, y)

.子程序 VNC_鼠标_左键弹起, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标 (序号, x, y, 0)

.子程序 VNC_鼠标_全部弹起, , 公开, 原子清除鼠标掩码并发送释放包
.参数 序号, 整数型, , 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 ()
.如果真结束
VNC_鼠标 (序号, 全_线程参数[序号].x, 全_线程参数[序号].y, 0)

.子程序 VNC_鼠标_左键按住, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标 (序号, x, y, 1)

.子程序 VNC_鼠标_左键单击, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

VNC_鼠标_左键按住 (序号, x, y)
程序_延时 (取随机数 (13, 36), 0)
VNC_鼠标_左键弹起 (序号, x, y)

.子程序 VNC_鼠标_移动, , 公开, 
.参数 序号, 整数型, , 
.参数 x, 整数型, , 
.参数 y, 整数型, , 

赋值 (全_线程参数[序号].x, x)
赋值 (全_线程参数[序号].y, y)
VNC_鼠标 (序号, 全_线程参数[序号].x, 全_线程参数[序号].y, -1)

.子程序 VNC_VNC_键盘, 逻辑型, , 键盘；返回真表示按键包已交给发送队列
.参数 序号, 整数型, , 
.参数 标志, 字节型, , 1=按下 0=放开
.参数 键码, 整数型, , 
.局部变量 发送结果, 逻辑型, , , 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 (假)
.如果真结束
.如果真 (小于或等于 (键码, 0))
    返回 (假)
.如果真结束
线程_进入许可区 (全_线程参数[序号].VNC许可)
.如果真 (或者 (小于或等于 (全_线程参数[序号].id, 0), 等于 (全_线程参数[序号].VNC连接就绪, 假)))
    ' 连接已销毁，标记断线等待掉线线程重连，避免按键静默丢失造成远端卡键
    赋值 (全_线程参数[序号].VNC断线标志, 真)
    线程_退出许可区 (全_线程参数[序号].VNC许可)
    返回 (假)
.如果真结束
赋值 (发送结果, 全_线程参数[序号].m_Client.发送字节集 (全_线程参数[序号].id, 相加 ({4}, 到字节集 (标志), {0, 0}, 到字节集 (htonl (键码)))))
.如果真 (等于 (发送结果, 假))
    赋值 (全_线程参数[序号].VNC连接就绪, 假)
    赋值 (全_线程参数[序号].VNC断线标志, 真)
    线程_退出许可区 (全_线程参数[序号].VNC许可)
    返回 (假)
.如果真结束
线程_退出许可区 (全_线程参数[序号].VNC许可)
返回 (真)

.子程序 VNC_键盘_弹起, , 公开, 
.参数 序号, 整数型, , 
.参数 key, 整数型, , 
.局部变量 键码, 整数型, , , 
.局部变量 i, 整数型, , , 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 ()
.如果真结束
.如果真 (或者 (小于或等于 (key, 0), 大于 (key, 256)))
    返回 ()
.如果真结束
赋值 (键码, 辅助_key取键码 (key))
.如果真 (小于或等于 (键码, 0))
    返回 ()
.如果真结束
' 弹起发送成功后才能删除状态；失败时留给重连握手继续补发
线程_进入许可区 (全_线程参数[序号].键状态锁)
.如果真 (VNC_VNC_键盘 (序号, 0, 键码))
    .计次循环首 (取数组成员数 (全_线程参数[序号].按住键), i)
        .如果真 (等于 (全_线程参数[序号].按住键[i], key))
            删除成员 (全_线程参数[序号].按住键, i, 1)
            跳出循环 ()
        .如果真结束
        
    .计次循环尾 ()
.如果真结束
线程_退出许可区 (全_线程参数[序号].键状态锁)

.子程序 VNC_键盘_全部弹起, , 公开, 释放当前线程所有按住的键，并补发脚本常用键，防止远端卡键
.参数 序号, 整数型, , 
.局部变量 i, 整数型, , , 
.局部变量 键码, 整数型, , , 
.局部变量 固定键, 整数型, , "0", 
.局部变量 仍按住键, 整数型, , "0", 

.如果真 (或者 (小于 (序号, 1), 大于 (序号, 取数组成员数 (全_线程参数))))
    返回 ()
.如果真结束
' 脚本实际用到的键全部补发一次弹起（未按住的键弹起无害，可解开远端卡键）
加入成员 (固定键, #Z键)
加入成员 (固定键, #A键)
加入成员 (固定键, #S键)
加入成员 (固定键, #D键)
加入成员 (固定键, #C键)
加入成员 (固定键, #Ctrl键)
加入成员 (固定键, #Shift键)
加入成员 (固定键, #Alt键)
加入成员 (固定键, 32)
加入成员 (固定键, 13)
加入成员 (固定键, 27)
加入成员 (固定键, 37)
加入成员 (固定键, 38)
加入成员 (固定键, 39)
加入成员 (固定键, 40)
加入成员 (固定键, 33)
加入成员 (固定键, 34)
加入成员 (固定键, 35)
加入成员 (固定键, 36)
加入成员 (固定键, #I键)
加入成员 (固定键, #K键)
加入成员 (固定键, #E键)
加入成员 (固定键, 8)
加入成员 (固定键, 220)
' 清键期间禁止其它线程插入新的按下；发送失败的键继续保留
线程_进入许可区 (全_线程参数[序号].键状态锁)
.计次循环首 (取数组成员数 (全_线程参数[序号].按住键), i)
    .如果真 (大于 (全_线程参数[序号].按住键[i], 0))
        赋值 (键码, 辅助_key取键码 (全_线程参数[序号].按住键[i]))
        .如果真 (并且 (大于 (键码, 0), 等于 (VNC_VNC_键盘 (序号, 0, 键码), 假)))
            加入成员 (仍按住键, 全_线程参数[序号].按住键[i])
        .如果真结束
        
    .如果真结束
    
.计次循环尾 ()
重定义数组 (全_线程参数[序号].按住键, 假, 0)
.计次循环首 (取数组成员数 (仍按住键), i)
    加入成员 (全_线程参数[序号].按住键, 仍按住键[i])
.计次循环尾 ()
' 固定清单直接发弹起，不误删清键过程中其它逻辑的状态
.计次循环首 (取数组成员数 (固定键), i)
    赋值 (键码, 辅助_key取键码 (固定键[i]))
    .如果真 (大于 (键码, 0))
        VNC_VNC_键盘 (序号, 0, 键码)
    .如果真结束
    
.计次循环尾 ()
线程_退出许可区 (全_线程参数[序号].键状态锁)

.子程序 VNC_键盘_输入字符, , 公开, 普通前台能输入中文，其它模式只能输入英文(大小写都可以),数字，和所有半角符号。出于用户体验考虑，绑定失败之后不会执行任何键盘鼠标动作。
.参数 序号, 整数型, , 
.参数 String, 文本型, , 欲输入的文本。
.参数 输入间隔, 整数型, 可空, 每次输入之间的间隔  默认25
.局部变量 Unicode, 字节集, , , 
.局部变量 原, 字节集, , , 
.局部变量 位置, 整数型, , , 
.局部变量 i, 整数型, , , 
.局部变量 当前字节, 字节型, , , 
.局部变量 编码, 文本型, , , 

.如果真 (是否为空 (输入间隔))
    赋值 (输入间隔, 25)
.如果真结束
赋值 (Unicode, 编码_Ansi到Unicode (String, ))
赋值 (原, 到字节集 (String))
赋值 (位置, 0)
.计次循环首 (相除 (取字节集长度 (Unicode), 2), i)
    赋值 (位置, 相加 (位置, 1))
    赋值 (当前字节, Unicode[相加 (相乘 (相减 (i, 1), 2), 1)])
    
    .判断开始 (不等于 (Unicode[相加 (相乘 (相减 (i, 1), 2), 2)], 0))
        
        赋值 (编码, 取编码 (到文本 (相加 (到字节集 (原[位置]), 到字节集 (原[相加 (位置, 1)]))), ))
        
        ' 调试输出 (编码)
        .判断开始 (不等于 (编码, “”))
            VNC_键盘_输入字符 (序号, 编码, )
        .默认
            VNC_键盘_按键 (序号, #Shift键)
            VNC_键盘_输入字符 (序号, 到半角 (到文本 (相加 (到字节集 (原[位置]), 到字节集 (原[相加 (位置, 1)])))), )
            VNC_键盘_按键 (序号, #Shift键)
        .判断结束
        赋值 (位置, 相加 (位置, 1))
    .判断 (并且 (大于或等于 (当前字节, 65), 小于或等于 (当前字节, 90)))
        .判断开始 (文本_是否大写字母 (到文本 (到字节集 (原[位置]))))
            VNC_键盘_按键 (序号, #CapsLock键)
            VNC_键盘_输入字符 (序号, 到小写 (到文本 (到字节集 (原[位置]))), )
            VNC_键盘_按键 (序号, #CapsLock键)
        .默认
            VNC_键盘_组合按键 (序号, Unicode[相加 (相乘 (相减 (i, 1), 2), 1)], 4)
        .判断结束
        
    .判断 (并且 (大于或等于 (当前字节, 97), 小于或等于 (当前字节, 122)))
        VNC_键盘_按键 (序号, 相减 (Unicode[相加 (相乘 (相减 (i, 1), 2), 1)], 32))
    .判断 (并且 (大于或等于 (当前字节, 48), 小于或等于 (当前字节, 57)))
        VNC_键盘_按键 (序号, Unicode[相加 (相乘 (相减 (i, 1), 2), 1)])
    .判断 (等于 (当前字节, 33))
        VNC_键盘_组合按键 (序号, 49, 4)
    .判断 (等于 (当前字节, 64))
        VNC_键盘_组合按键 (序号, 50, 4)
        
    .判断 (等于 (当前字节, 35))
        VNC_键盘_组合按键 (序号, 51, 4)
        
    .判断 (等于 (当前字节, 36))
        VNC_键盘_组合按键 (序号, 52, 4)
        
    .判断 (等于 (当前字节, 37))
        VNC_键盘_组合按键 (序号, 52, 4)
        
    .判断 (等于 (当前字节, 94))
        VNC_键盘_组合按键 (序号, 54, 4)
    .判断 (等于 (当前字节, 38))
        VNC_键盘_组合按键 (序号, 55, 4)
    .判断 (等于 (当前字节, 42))
        VNC_键盘_组合按键 (序号, 56, 4)
    .判断 (等于 (当前字节, 40))
        VNC_键盘_组合按键 (序号, 57, 4)
    .判断 (等于 (当前字节, 41))
        VNC_键盘_组合按键 (序号, 48, 4)
    .判断 (等于 (当前字节, 45))
        VNC_键盘_按键 (序号, 189)
    .判断 (等于 (当前字节, 95))
        VNC_键盘_组合按键 (序号, 189, 4)
    .判断 (等于 (当前字节, 43))
        VNC_键盘_组合按键 (序号, 187, 4)
    .判断 (等于 (当前字节, 61))
        VNC_键盘_按键 (序号, 187)
    .判断 (等于 (当前字节, 91))
        VNC_键盘_按键 (序号, 219)
    .判断 (等于 (当前字节, 92))
        VNC_键盘_按键 (序号, 220)
    .判断 (等于 (当前字节, 93))
        VNC_键盘_按键 (序号, 221)
    .判断 (等于 (当前字节, 59))
        VNC_键盘_按键 (序号, 186)
    .判断 (等于 (当前字节, 44))
        VNC_键盘_按键 (序号, 188)
    .判断 (等于 (当前字节, 46))
        VNC_键盘_按键 (序号, 190)
    .判断 (等于 (当前字节, 47))
        VNC_键盘_按键 (序号, 191)
    .判断 (等于 (当前字节, 39))
        VNC_键盘_按键 (序号, 222)
    .判断 (等于 (当前字节, 123))
        VNC_键盘_组合按键 (序号, 219, 4)
    .判断 (等于 (当前字节, 124))
        VNC_键盘_组合按键 (序号, 220, 4)
    .判断 (等于 (当前字节, 125))
        VNC_键盘_组合按键 (序号, 221, 4)
    .判断 (等于 (当前字节, 58))
        VNC_键盘_组合按键 (序号, 186, 4)
    .判断 (等于 (当前字节, 60))
        VNC_键盘_组合按键 (序号, 188, 4)
    .判断 (等于 (当前字节, 62))
        VNC_键盘_组合按键 (序号, 190, 4)
    .判断 (等于 (当前字节, 63))
        VNC_键盘_组合按键 (序号, 191, 4)
    .判断 (等于 (当前字节, 34))
        VNC_键盘_组合按键 (序号, 222, 4)
    .判断 (等于 (当前字节, 96))
        VNC_键盘_按键 (序号, 192)
    .判断 (等于 (当前字节, 126))
        VNC_键盘_组合按键 (序号, 192, 4)
    .判断 (等于 (当前字节, 32))
        VNC_键盘_按键 (序号, 32)
    .默认
        
    .判断结束
    程序_延时 (输入间隔, )
.计次循环尾 ()

.子程序 VNC_键盘_按键, , 公开, 
.参数 序号, 整数型, , 
.参数 key, 整数型, , 

.如果真 (或者 (小于或等于 (key, 0), 大于 (key, 256)))
    返回 ()
.如果真结束
VNC_键盘_按下 (序号, key)
程序_延时 (10, )
VNC_键盘_弹起 (序号, key)
程序_延时 (10, )

.子程序 VNC_键盘_组合按键, , 公开, 
.参数 序号, 整数型, , 
.参数 key, 整数型, , 
.参数 状态键, 整数型, , 1=Alt,2=Ctrl,4=Shift  各值可相加。
.局部变量 键码, 整数型, , , 
.局部变量 键_按下, 整数型, , "0", 
.局部变量 键_松开, 整数型, , "0", 
.局部变量 i, 整数型, , , 

赋值 (键码, 辅助_key取键码 (key))
.如果真 (位测试 (状态键, 1))
    加入成员 (键_按下, #Alt键)
    加入成员 (键_松开, #Alt键)
.如果真结束
.如果真 (位测试 (状态键, 2))
    加入成员 (键_按下, #Ctrl键)
    加入成员 (键_松开, #Ctrl键)
.如果真结束
.如果真 (位测试 (状态键, 3))
    加入成员 (键_按下, #Shift键)
    加入成员 (键_松开, #Shift键)
.如果真结束
加入成员 (键_按下, key)
加入成员 (键_松开, key)
.计次循环首 (取数组成员数 (键_按下), i)
    VNC_键盘_按下 (序号, 键_按下[i])
    程序_延时 (25, )
.计次循环尾 ()
.计次循环首 (取数组成员数 (键_松开), i)
    VNC_键盘_弹起 (序号, 键_松开[i])
    程序_延时 (25, )
.计次循环尾 ()

.子程序 辅助_key取键码, 整数型, , 
.参数 key, 整数型, , 
.局部变量 键码, 整数型, , , 
.局部变量 ls, 双精度小数型, , , 

.判断开始 (并且 (大于 (key, 47), 小于 (key, 91)))  ' 字母区+字母区数字
    赋值 (键码, key)
.判断 (并且 (大于 (key, 95), 小于 (key, 106)))  ' 数字区
    赋值 (ls, 相减 (key, 96))
    赋值 (键码, 相加 (65456, ls))
.判断 (并且 (大于 (key, 105), 小于 (key, 112)))  ' 数字区符号
    赋值 (ls, 相减 (key, 106))
    赋值 (键码, 相加 (65450, ls))
.判断 (并且 (大于 (key, 111), 小于 (key, 124)))
    赋值 (ls, 相减 (key, 112))
    赋值 (键码, 相加 (65470, ls))
.判断 (等于 (key, 8))
    赋值 (键码, 65288)
.判断 (等于 (key, 9))
    赋值 (键码, 65289)
.判断 (等于 (key, 12))
    赋值 (键码, 65291)
.判断 (等于 (key, 13))
    赋值 (键码, 65293)
.判断 (并且 (大于 (key, 15), 小于 (key, 18)))
    赋值 (ls, 相减 (key, 15))
    赋值 (键码, 相加 (65505, ls))
.判断 (等于 (key, 18))
    赋值 (ls, 相减 (key, 164))
    赋值 (键码, 65513)
.判断 (等于 (key, 20))
    赋值 (键码, 65509)
.判断 (等于 (key, 27))
    赋值 (键码, 65307)
.判断 (等于 (key, 32))
    赋值 (键码, key)
.判断 (等于 (key, 33))
    赋值 (键码, 65365)
.判断 (等于 (key, 34))
    赋值 (键码, 65366)
.判断 (等于 (key, 35))
    赋值 (键码, 65367)
.判断 (等于 (key, 36))
    赋值 (键码, 65360)
.判断 (并且 (大于 (key, 36), 小于 (key, 41)))
    赋值 (ls, 相减 (key, 37))
    赋值 (键码, 相加 (65361, ls))
.判断 (等于 (key, 44))
    赋值 (键码, 65377)
.判断 (等于 (key, 45))
    赋值 (键码, 65379)
.判断 (等于 (key, 46))
    赋值 (键码, 65535)
.判断 (等于 (key, 144))
    赋值 (键码, 65407)
.判断 (等于 (key, 222))
    赋值 (键码, 39)
.判断 (等于 (key, 186))
    赋值 (键码, 59)
.判断 (等于 (key, 188))
    赋值 (键码, 60)
.判断 (等于 (key, 187))
    赋值 (键码, 61)
.判断 (等于 (key, 190))
    赋值 (键码, 62)
.判断 (等于 (key, 191))
    赋值 (键码, 63)
.判断 (等于 (key, 219))
    赋值 (键码, 91)
.判断 (等于 (key, 220))
    赋值 (键码, 92)
.判断 (等于 (key, 221))
    赋值 (键码, 93)
.判断 (等于 (key, 189))
    赋值 (键码, 95)
.判断 (等于 (key, 192))
    赋值 (键码, 96)
.默认
    
.判断结束
返回 (键码)

.子程序 转键代码, 整数型, , 
.参数 线程序号, , , 
.参数 输入字符, 文本型, , 

.判断开始 (等于 (输入字符, “a”))
    返回 (65)
.判断 (等于 (输入字符, “b”))
    返回 (66)
.判断 (等于 (输入字符, “c”))
    返回 (67)
.判断 (等于 (输入字符, “d”))
    返回 (68)
.判断 (等于 (输入字符, “e”))
    返回 (69)
.判断 (等于 (输入字符, “f”))
    返回 (70)
.判断 (等于 (输入字符, “g”))
    返回 (71)
.判断 (等于 (输入字符, “h”))
    返回 (72)
.判断 (等于 (输入字符, “i”))
    返回 (73)
.判断 (等于 (输入字符, “j”))
    返回 (74)
.判断 (等于 (输入字符, “k”))
    返回 (75)
.判断 (等于 (输入字符, “l”))
    返回 (76)
.判断 (等于 (输入字符, “m”))
    返回 (77)
.判断 (等于 (输入字符, “n”))
    返回 (78)
.判断 (等于 (输入字符, “o”))
    返回 (79)
.判断 (等于 (输入字符, “p”))
    返回 (80)
.判断 (等于 (输入字符, “q”))
    返回 (81)
.判断 (等于 (输入字符, “r”))
    返回 (82)
.判断 (等于 (输入字符, “s”))
    返回 (83)
.判断 (等于 (输入字符, “t”))
    返回 (84)
.判断 (等于 (输入字符, “u”))
    返回 (85)
.判断 (等于 (输入字符, “v”))
    返回 (86)
.判断 (等于 (输入字符, “w”))
    返回 (87)
.判断 (等于 (输入字符, “x”))
    返回 (88)
.判断 (等于 (输入字符, “y”))
    返回 (89)
.判断 (等于 (输入字符, “z”))
    返回 (90)
.判断 (等于 (输入字符, “1”))
    返回 (49)
.判断 (等于 (输入字符, “0”))
    返回 (48)
.判断 (等于 (输入字符, “2”))
    返回 (50)
.判断 (等于 (输入字符, “3”))
    返回 (51)
.判断 (等于 (输入字符, “4”))
    返回 (52)
.判断 (等于 (输入字符, “5”))
    返回 (53)
.判断 (等于 (输入字符, “6”))
    返回 (54)
.判断 (等于 (输入字符, “7”))
    返回 (55)
.判断 (等于 (输入字符, “8”))
    返回 (56)
.判断 (等于 (输入字符, “9”))
    返回 (57)
.判断 (等于 (输入字符, “.”))
    返回 (190)
.判断 (等于 (输入字符, “@”))
    返回 (999)
.默认
    
.判断结束

返回 (0)

