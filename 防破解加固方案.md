# 防破解加固方案（VNC多线程散读快速）

> 针对当前实现：卡密云验证（machine_code + card_password）＋ 机器码绑定 ＋ HTTP POST ＋ 心跳检测 ＋ VMP 加壳。
> 以下所有代码均为易语言语法，可直接粘贴；服务端配合项标注【服务端】。

---

## 一、现状与薄弱点（按破解者视角）

| # | 薄弱点 | 破解者的利用方式 | 严重度 |
|---|--------|------------------|--------|
| 1 | 关键 URL/参数明文存储（`/index/card_password_login`、`machine_code`、`data.token` 等） | 直接搜字符串定位验证逻辑 | 高 |
| 2 | 验证结果集中在单一判断点 | OD 下断点 / NOP 掉跳转即可放行 | 高 |
| 3 | HTTP 无时间戳签名 | 重放登录包 / 伪造返回 | 高 |
| 4 | token、到期时间全局明文 | 内存搜索直接改时间 | 中 |
| 5 | 无反调试 / 反调试不完整 | OD/x64dbg 从容下断分析 | 中 |
| 6 | VMP 仅整体加壳，验证代码未虚拟化 | 壳外下断点后逻辑一览无余 | 高 |
| 7 | 无完整性自校验 | 改完 exe 直接运行 | 中 |
| 8 | 机器码来源单一 | 模拟固定机器码骗过绑定 | 中 |

加固原则：**不要把验证做成"一个判断、一个跳转"，要让破解者 patch 一处、全盘崩溃**。

---

## 二、模块 1：字符串加密（隐藏关键 URL / 参数）

### 2.1 解密子程序（新建一个程序集"防破解"，放这些子程序）

```易语言
.版本 2
.程序集 防破解

.子程序 防破解_解密文本, 文本型, 公开, 密文字节集 → 明文
.参数 密文, 字节集
.局部变量 密钥, 字节集
.局部变量 i, 整数型
.局部变量 密钥长度, 整数型
.局部变量 密文长度, 整数型

    密钥 ＝ 到字节集 (“MXD-D4HX9-2026”)
    密钥长度 ＝ 取字节集长度 (密钥)
    密文长度 ＝ 取字节集长度 (密文)
    如果真 (密文长度 ＝ 0)
        返回 (“”)
    结束如果
    计次循环首 (密文长度, i)
        密文 [i] ＝ 位异或 (密文 [i], 密钥 [(i－1) ％ 密钥长度 ＋ 1])
    计次循环尾 ()
    返回 (到文本 (密文))
```

### 2.2 把明文全部替换为解密调用（替换表）

| 原文 | 替换为 |
|------|--------|
| `/index/card_password_login` | `防破解_解密文本 ({98,49,42,73,33,76,103,59,88,95,86,111,66,87,62,43,51,66,54,80,23,52,86,74,91,94})` |
| `/index/card_password_check` | `防破解_解密文本 ({98,49,42,73,33,76,103,59,88,95,86,111,66,87,62,43,51,66,54,80,23,59,81,72,81,91})` |
| `/index/card_password_bind` | `防破解_解密文本 ({98,49,42,73,33,76,103,59,88,95,86,111,66,87,62,43,51,66,54,80,23,58,80,67,86})` |
| `machine_code` | `防破解_解密文本 ({32,57,39,69,45,90,45,7,90,66,86,85})` |
| `card_password` | `防破解_解密文本 ({46,57,54,73,27,68,41,43,74,90,93,66,86})` |
| `data.token` | `防破解_解密文本 ({41,57,48,76,106,64,39,51,92,67})` |
| `data.expire_time` | `防破解_解密文本 ({41,57,48,76,106,81,48,40,80,95,87,111,70,95,32,61})` |
| `暗黑4` | `防破解_解密文本 ({253,237,254,247,112})` |
| `登录成功` | `防破解_解密文本 ({248,159,134,145,247,253,241,254})` |
| `卡密到期` | `防破解_解密文本 ({242,240,135,241,241,137,142,130})` |
| `卡密掉线` | `防破解_解密文本 ({242,240,135,241,241,192,135,135})` |
| `到期了` | `防破解_解密文本 ({248,229,130,247,133,255})` |
| `C:\挂机设置.txt` | `防破解_解密文本 ({14,98,24,148,150,143,178,145,209,251,241,30,70,78,57})` |
| `检测到调试器，程序即将退出！` | `防破解_解密文本 ({241,180,246,207,241,137,253,175,243,249,244,199,145,154,254,148,148,223,248,128,245,243,244,230,129,198,145,151})` |
| `登录中...` | `防破解_解密文本 ({248,159,134,145,146,228,102,118,23})` |
| `验证成功` | `防破解_解密文本 ({156,177,146,137,247,253,241,254})` |

> 说明：
> - 密钥 `MXD-D4HX9-2026` 是明文出现在代码里的，破解者仍能通过调试还原。**真正目标不是"解不开"，而是让字符串搜索/静态分析失效，逼他动态调试**——动态调试交给 VMP 虚拟化 + 反调试去挡。
> - 建议每次发布前换一个密钥并重新生成密文（用"模块 3 的签名盐"保持一致）。

---

## 三、模块 2：反调试综合检测

### 3.1 API 声明（放在程序集顶部）

```易语言
.DLL命令 API_IsDebuggerPresent, 整数型, "kernel32.dll", "IsDebuggerPresent", , , 公开
.DLL命令 API_GetCurrentProcess, 整数型, "kernel32.dll", "GetCurrentProcess", , , 公开
.DLL命令 API_CheckRemoteDebuggerPresent, 整数型, "kernel32.dll", "CheckRemoteDebuggerPresent", , , 公开
    .参数 hProcess, 整数型
    .参数 pbDebuggerPresent, 整数型
.DLL命令 API_NtQueryInformationProcess, 整数型, "ntdll.dll", "NtQueryInformationProcess", , , 公开
    .参数 ProcessHandle, 整数型
    .参数 ProcessInformationClass, 整数型
    .参数 ProcessInformation, 整数型
    .参数 ProcessInformationLength, 整数型
    .参数 ReturnLength, 整数型
```

### 3.2 读取 PEB.NtGlobalFlag（绕过 API HOOK）

```易语言
.子程序 防破解_取NtGlobalFlag, 整数型, 公开, 返回 PEB 偏移 0x68 处的值
.局部变量 局_标志, 整数型
    置入代码 ({ 100, 139, 13, 48, 0, 0, 0, 139, 65, 104, 137, 69, 252 })
    返回 (局_标志)
```

### 3.3 综合检测子程序

```易语言
.子程序 防破解_反调试检测, 逻辑型, 公开, 返回真 = 检测到调试环境
.局部变量 局_句柄, 整数型
.局部变量 局_标志, 整数型
.局部变量 局_调试端口, 整数型
.局部变量 局_全局标志, 整数型
.局部变量 局_进程名数组, 文本型, , "0"
.局部变量 局_窗口名数组, 文本型, , "0"
.局部变量 局_i, 整数型

    ' ① IsDebuggerPresent
    如果真 (API_IsDebuggerPresent () ≠ 0)
        返回 (真)
    结束如果

    ' ② CheckRemoteDebuggerPresent
    局_标志 ＝ 0
    API_CheckRemoteDebuggerPresent (API_GetCurrentProcess (), 取变量地址 (局_标志))
    如果真 (局_标志 ≠ 0)
        返回 (真)
    结束如果

    ' ③ NtQueryInformationProcess 查调试端口 (ProcessDebugPort = 7)
    局_调试端口 ＝ 0
    API_NtQueryInformationProcess (API_GetCurrentProcess (), 7, 取变量地址 (局_调试端口), 4, 0)
    如果真 (局_调试端口 ≠ 0)
        返回 (真)
    结束如果

    ' ④ PEB.NtGlobalFlag (位与 0x70)
    局_全局标志 ＝ 防破解_取NtGlobalFlag ()
    如果真 (位与 (局_全局标志, 112) ≠ 0)
        返回 (真)
    结束如果

    ' ⑤ 常见逆向工具进程
    局_进程名数组 ＝ { “x64dbg.exe”, “x32dbg.exe”, “ollydbg.exe”, “ollyice.exe”, “cheatengine.exe”, “cheatengine-x86_64.exe”, “cheatengine-x86_32.exe”, “ida.exe”, “ida64.exe”, “windbg.exe”, “immunitydebugger.exe”, “scylla.exe”, “ImportREC.exe”, “LordPE.exe”, “DnSpy.exe”, “VMPDump.exe” }
    计次循环首 (取数组成员数 (局_进程名数组), 局_i)
        如果真 (进程_是否存在 (局_进程名数组 [局_i]))
            返回 (真)
        结束如果
    计次循环尾 ()

    ' ⑥ 调试器窗口特征
    局_窗口名数组 ＝ { “x64dbg”, “x32dbg”, “OllyDbg”, “Immunity Debugger”, “Cheat Engine”, “IDA -” }
    计次循环首 (取数组成员数 (局_窗口名数组), 局_i)
        如果真 (窗口_是否存在 (局_窗口名数组 [局_i]))
            返回 (真)
        结束如果
    计次循环尾 ()

    返回 (假)
```

### 3.4 反调试的调用策略（重要）

**检测到调试器后不要立即退出**——破解者会在 `ExitProcess` 处下断点，一退就知道被你发现了。推荐"假死"策略：

```易语言
.子程序 防破解_调试器响应, , 公开, 检测到调试后的处理
.局部变量 局_延时, 整数型

    ' ① 设置全局"已受污染"标志，后续业务逻辑自动错乱（见模块 5）
    全_调试被检测 ＝ 真

    ' ② 随机延时 2~8 秒再退出，制造"偶发崩溃"假象
    置随机种子 (取现行时间 ())
    局_延时 ＝ 2000 ＋ 取随机数 (0, 6000)
    延时 (局_延时)

    ' ③ 破坏本地状态再退出
    删除注册项 (2, “SOFTWARE\MXD\v9”)
    结束 ()
```

调用位置：
- 启动窗口创建完毕后调用一次；
- 心跳线程里**随机间隔**再调（不固定周期，防节奏分析）。

---

## 四、模块 3：云验证加签名（防重放 / 防伪造）

> 你现在的 POST 是裸参数，抓包重放就能拿到同样的返回。加上"时间戳 + 签名"后，重放超过 5 分钟的包一律无效。

### 4.1 客户端签名函数

```易语言
.子程序 防破解_取时间戳, 文本型, 公开, 返回秒级时间戳
    .局部变量 局_起始, 日期时间型
    局_起始 ＝ 到时间 (“1970-01-01 08:00:00”)
    返回 (到文本 (取时间间隔 (取现行时间 (), 局_起始, #秒)))
```

```易语言
.子程序 防破解_取签名, 文本型, 公开, HMAC 简化版：MD5(卡密+机器码+时间戳+盐)
.参数 卡密, 文本型
.参数 机器码, 文本型
.参数 时间戳, 文本型
.局部变量 盐, 文本型

    盐 ＝ 防破解_解密文本 ({9,108,12,117,125,25,112,30,11,110,31,7,115,7,8})   ' 明文 D4HX9-8F2C-7A1E，服务端同一密钥，发布时更换
    返回 (取数据摘要 (到字节集 (卡密 ＋ 机器码 ＋ 时间戳 ＋ 盐)))
```

### 4.2 改造登录 / 心跳 / 解绑请求

```易语言
.子程序 防破解_登录请求, 文本型, 公开, 返回服务器返回原文
.参数 卡密, 文本型
.参数 机器码, 文本型
.局部变量 时间戳, 文本型
.局部变量 签名, 文本型
.局部变量 POST地址, 文本型
.局部变量 POST数据, 文本型

    时间戳 ＝ 防破解_取时间戳 ()
    签名 ＝ 防破解_取签名 (卡密, 机器码, 时间戳)

    POST地址 ＝ 服务器地址 ＋ 防破解_解密文本 ({98,49,42,73,33,76,103,59,88,95,86,111,66,87,62,43,51,66,54,80,23,52,86,74,91,94})
    POST数据 ＝ 防破解_解密文本 ({32,57,39,69,45,90,45,7,90,66,86,85}) ＋ “=” ＋ 机器码
        ＋ “&” ＋ 防破解_解密文本 ({46,57,54,73,27,68,41,43,74,90,93,66,86}) ＋ “=” ＋ 卡密
        ＋ “&map=” ＋ 防破解_解密文本 ({253,237,254,247,112})
        ＋ “&money=” ＋ 防破解_解密文本 ({253,237,254,247,112})
        ＋ “&timestamp=” ＋ 时间戳
        ＋ “&sign=” ＋ 签名

    返回 (网页_访问S (POST地址, 1, POST数据, , , , , , , , , , , , , ))
```

> `网页_访问S` 是精易模块命令，返回 UTF-8 文本；你的原代码里用的 `网页_访问` 系列按你的模块版本调整。

### 4.3 【服务端】必须配合的校验（这是关键，只改客户端没用）

1. 收到请求先算 `sign = MD5(card_password + machine_code + timestamp + 盐)`，不一致直接拒绝；
2. 校验 `|当前时间 - timestamp| ≤ 300 秒`，超时拒绝（防重放）；
3. 同一 timestamp 只能使用一次（用 Redis/数据库做 nonce 去重，防秒内重放）；
4. 登录 / 心跳 / 解绑三个接口统一加这套校验；
5. **建议整体换 HTTPS**，杜绝中间人抓明文（签名能挡重放，但挡不住抓包看逻辑，HTTPS 一起上）。

---

## 五、模块 4：token / 到期时间本地加密存储

### 5.1 加密存储子程序

```易语言
.子程序 防破解_保存会话, , 公开, 登录成功后调用
.参数 令牌, 文本型
.参数 到期时间, 日期时间型
.局部变量 密文令牌, 字节集
.局部变量 密文到期, 字节集

    密文令牌 ＝ 防破解_加密字节集 (到字节集 (令牌))
    密文到期 ＝ 防破解_加密字节集 (到字节集 (到文本 (到期时间)))
    写注册项 (2, “SOFTWARE\MXD\v9”, “tk”, 密文令牌)
    写注册项 (2, “SOFTWARE\MXD\v9”, “et”, 密文到期)

    ' 关键：明文只在内存里短暂存在，用完后立即覆盖
    令牌 ＝ 取空白文本 (取文本长度 (令牌))
    到期时间 ＝ 到时间 (“1970-01-01 08:00:00”)
```

```易语言
.子程序 防破解_加密字节集, 字节集, 公开
.参数 原文, 字节集
.局部变量 密钥, 字节集
.局部变量 i, 整数型
.局部变量 长度, 整数型
.局部变量 随机头, 字节集

    密钥 ＝ 到字节集 (“MXD-D4HX9-2026”)
    长度 ＝ 取字节集长度 (原文)
    如果真 (长度 ＝ 0)
        返回 ({})
    结束如果
    ' 加 1~4 字节随机头，使每次密文不同（防固定密文比对）
    置随机种子 (取现行时间 ())
    随机头 ＝ 取随机数 (1, 255) 的字节集形式  ' 见下方替代写法
    ' 简化写法：直接异或
    计次循环首 (长度, i)
        原文 [i] ＝ 位异或 (原文 [i], 密钥 [(i－1) ％ 取字节集长度 (密钥) ＋ 1])
    计次循环尾 ()
    返回 (原文)
```

> 随机头写法：易语言里可用 `到字节集 (取随机数 (1, 255))` 得到一个字节，拼接即可，此处示意省略。

### 5.2 启动时恢复并校验

```易语言
.子程序 防破解_读取会话, 逻辑型, 公开, 返回真 = 本地会话有效
.局部变量 密文令牌, 字节集
.局部变量 密文到期, 文本型
.局部变量 到期, 日期时间型

    密文令牌 ＝ 读注册项 (2, “SOFTWARE\MXD\v9”, “tk”, )
    密文到期 ＝ 读注册项 (2, “SOFTWARE\MXD\v9”, “et”, )
    如果真 (取字节集长度 (密文令牌) ＝ 0)
        返回 (假)
    结束如果

    到期 ＝ 到时间 (到文本 (防破解_解密字节集 (密文到期)))
    如果真 (到期 ≤ 取现行时间 ())
        返回 (假)   ' 已到期，走重新登录
    结束如果

    全_令牌 ＝ 到文本 (防破解_解密字节集 (密文令牌))
    全_到期时间 ＝ 到期
    返回 (真)
```

> `防破解_解密字节集` 与 `防破解_加密字节集` 互为逆运算（异或对称，同一个函数即可）。

---

## 六、模块 5：验证结果与业务逻辑耦合（防 patch 核心）

**思路**：不要"验证通过才干活"，而是把验证结果**算进业务数据**里。破解者 NOP 掉验证后，业务数据全是错的，程序"看似解锁、实际瘫痪"。

示例（以你的内存读取偏移为例）：

```易语言
.子程序 防破解_派生密钥, 文本型, 公开, 登录成功后调用一次，得到业务种子
.局部变量 混合源, 文本型
    混合源 ＝ 全_令牌 ＋ 全_机器码 ＋ 到文本 (全_到期时间)
    返回 (取数据摘要 (到字节集 (混合源)))
```

```易语言
.子程序 防破解_偏移混淆, 整数型, 公开, 用派生密钥对内存偏移做混淆
.参数 原始偏移, 整数型
.局部变量 种子, 整数型
.局部变量 密钥串, 文本型

    ' 全_派生密钥 是登录成功后由 防破解_派生密钥 生成并全局保存
    密钥串 ＝ 全_派生密钥
    种子 ＝ 取字节集数据 (取字节集左边 (到字节集 (密钥串), 4), #整数型, 1)
    返回 (位异或 (原始偏移, 种子))
```

**落地方式**：
1. 登录成功后算一次 `全_派生密钥 ＝ 防破解_派生密钥 ()`；
2. 所有内存基址/偏移在使用时经过 `防破解_偏移混淆`；
3. 破解者 patch 掉验证 → `全_令牌` 为空 → `全_派生密钥` 为空串 → `取字节集数据` 返回 0 → 偏移全错 → 程序读不到内存，等于废了。

同样道理可以应用在：
- 线程数 / 调度参数；
- 验证返回 code 的判断散落到 3~5 处，而不是一个 `如果` ；
- 心跳返回的"卡密掉线 / 到期 / 多开"分别触发不同的污染行为（停止写内存、清空偏移、延时后退出），别让它们都走同一个退出路径。

---

## 七、模块 6：VMP 强化（把关键代码真正虚拟化）

你已经在用 VMP（目录里有 `MXD.exe.vmp` 配置和 `MXD.vmp.exe`），但**只加壳不够**——加壳后破解者照样在壳外下断点分析。必须把验证/加密/反调试这些关键函数"虚拟化"。

### 7.1 易语言接入 VMP SDK

用 VMProtectSDK32.dll（VMP 安装目录里有），声明：

```易语言
.DLL命令 VMP_虚拟化开始, , "VMProtectSDK32.dll", "VMProtectBeginVirtualization", , , 公开
    .参数 标记名, 文本型
.DLL命令 VMP_变异开始, , "VMProtectSDK32.dll", "VMProtectBeginMutation", , , 公开
    .参数 标记名, 文本型
.DLL命令 VMP_超强开始, , "VMProtectSDK32.dll", "VMProtectBeginUltra", , , 公开
    .参数 标记名, 文本型
.DLL命令 VMP_结束, , "VMProtectSDK32.dll", "VMProtectEnd", , , 公开
```

### 7.2 使用方式（包裹关键子程序）

```易语言
.子程序 防破解_核心验证, 逻辑型, 公开
    VMP_虚拟化开始 (“verify_core”)   ' 虚拟化：指令翻译成虚拟机字节码，极难还原

    ' —— 这里放真正的验证代码：签名校验、token 解密、心跳判断 ——
    ' （把模块 3 / 4 的关键逻辑都挪进来）

    VMP_结束 ()
    返回 (验证结果)
```

建议**虚拟化**（而非仅变异）这些函数：
- 防破解_取签名
- 防破解_解密文本 / 防破解_加密字节集
- 防破解_核心验证（登录/心跳的最终判断）
- 防破解_偏移混淆

### 7.3 VMP 加壳 GUI 配置（发布打包）

1. 打开 VMProtect，拖入编译好的 exe；
2. 左侧选中**函数**：把上面几个子程序（导出符号）设为「虚拟化」；其余函数可设「变异」；
3. 选项：
   - ✔ 虚拟化 ✔ 变异 ✔ 内存保护 ✔ 压缩输出
   - 加强模式（Ultra）对核心验证函数开启
4. **每次发布换一个新的序列号密钥**（选项里有"序列号/密钥"配置）；
5. 加壳后务必自己跑一遍登录全流程验证功能没坏；
6. 不要对全程序开"超强虚拟化"（Ultra 全开会拖慢 + 易崩），只对关键函数开。

---

## 八、机器码加固（让绑定无法被模拟）

```易语言
.子程序 防破解_取机器码, 文本型, 公开, 多源混合，返回 32 位 MD5
.局部变量 硬盘, 文本型
.局部变量 CPU, 文本型
.局部变量 网卡, 文本型
.局部变量 主板, 文本型
.局部变量 卷标, 文本型
.局部变量 合成串, 文本型

    硬盘 ＝ 系统_取硬盘特征字 ()          ' 精易模块
    CPU ＝ 取CPU信息 ()                   ' 按你的精易版本取 CPU ID
    网卡 ＝ 系统_取MAC地址 ()             ' 精易模块
    主板 ＝ 系统_取主板序列号 ()          ' 精易模块（若没有可去掉）
    卷标 ＝ 系统_取磁盘卷标 (“C:”)        ' 精易模块

    合成串 ＝ 硬盘 ＋ “|” ＋ CPU ＋ “|” ＋ 网卡 ＋ “|” ＋ 主板 ＋ “|” ＋ 卷标
    返回 (取数据摘要 (到字节集 (合成串)))
```

> 注意：合成串里加入随机盐会导绑定时和登录时机器码不一致，**不要加随机盐**，保持确定性。
> 【服务端】绑定时记录这个机器码 MD5；登录/心跳时核对一致，不一致即判定为"模拟机器码"或换机，按你的规则处理。

---

## 九、发布打包流程（每次发版照做）

1. 换掉字符串密钥 + 签名盐（模块 1 / 3 的两处密钥，服务端同步）→ 重新生成密文替换表；
2. 用 VMP 打开编译好的 exe → 关键函数设虚拟化 → 换新序列号密钥 → 加壳；
3. 加壳后先本地跑通：登录、心跳、解绑、到期自动退出，全流程过一遍；
4. 用 x64dbg 附加自己试一下：验证反调试能触发、字符串搜不到明文 URL；
5. 把新 exe 的 MD5 登记到服务端（如果做了服务端完整性校验）；
6. 分发给用户。

---

## 十、服务端改动清单（这步不跟上，客户端再强也白搭）

- [ ] 三个接口（login / check / bind）全部校验 `timestamp + sign`，超时与重放拒绝；
- [ ] 换 HTTPS（买证书或用 CDN 免费证书）；
- [ ] 机器码与绑定记录核对；
- [ ] 卡密状态（在线/到期/多开）以服务端为准，客户端只做展示；
- [ ] 可选：服务端登记每版 exe 的 MD5，客户端启动时上报比对，未登记的版本拒绝服务。
