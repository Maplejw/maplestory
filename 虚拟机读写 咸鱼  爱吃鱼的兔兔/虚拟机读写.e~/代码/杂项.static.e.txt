.程序集 杂项, , , 
.程序集变量 集_基本路径, 文本型, , , 
.程序集变量 集_官方Vmm路径, 文本型, , , 
.程序集变量 集_空程序路径, 文本型, , , 
.程序集变量 集_官方COL路径, 文本型, , , 


.子程序 初始化VMware, 逻辑型, , 初始化 虚拟机 官方文档dll
.参数 参试次数, 整数型, , 
.局部变量 创建结果, 整数型, , , 
.局部变量 加载结果, 长整数型, , , 
.局部变量 枚举结果, 枚举导出函数, , "0", 
.局部变量 i, 整数型, , , 

.如果真 (取反 (是否首次释放))
    赋值 (是否首次释放, 真)
    赋值 (集_基本路径, 相加 (取特定目录 (11), “ZCK”, 到文本 (取启动时间_Ex ())))
    赋值 (集_官方Vmm路径, 相加 (集_基本路径, “\vmm.dll”))
    赋值 (集_空程序路径, 相加 (集_基本路径, “\Microsoft”, 到文本 (GetCurrentProcessId ()), “.exe”))
    赋值 (集_官方COL路径, 相加 (集_基本路径, “\leechcore.dll”))
    MoveFileExA (集_基本路径, 0, 4)  ' 重启自删
    
    .如果真 (取反 (文件是否存在 (集_基本路径)))
        创建目录 (集_基本路径)
        程序_延时 (300, )
    .如果真结束
    .如果真 (取反 (文件是否存在 (集_官方Vmm路径)))
        写到文件 (集_官方Vmm路径, #vmm)
    .如果真结束
    .如果真 (取反 (文件是否存在 (集_空程序路径)))
        写到文件 (集_空程序路径, #桥接)
    .如果真结束
    .如果真 (取反 (文件是否存在 (集_官方COL路径)))
        写到文件 (集_官方COL路径, #leechore)
    .如果真结束
    程序_延时 (1000, )
    .如果真 (或者 (取反 (文件是否存在 (集_基本路径)), 取反 (文件是否存在 (集_官方Vmm路径)), 取反 (文件是否存在 (集_空程序路径)), 取反 (文件是否存在 (集_官方COL路径))))
        信息框 (“支持库释放失败！（请确保程序启动权限是否为管理员）”, 相加 (0, #错误图标, #位于前台), “错误：”, )
        返回 (假)
    .如果真结束
    
.如果真结束
.如果真 (等于 (help_初始化模块 (), 0))
    信息框 (“X64模块初始化失败！”, 相加 (0, #错误图标, #位于前台), “错误：”, )
    返回 (假)
.如果真结束

赋值 (创建结果, 进程_创建Ex (集_空程序路径, 相加 (“ ”, 到文本 (GetCurrentProcessId ())), 假, #隐藏窗口, 集_基本路径, 全_精易进程结构))
' 信息框 (“创建结果 ” ＋ 到文本 (创建结果), 0, , )
程序_延时 (500, )
.如果真 (大于 (创建结果, 0))
    赋值 (加载结果, 加载支持库 ())
    ' 信息框 (“加载结果 ” ＋ 到文本 (加载结果), 0, , )
    程序_延时 (500, )
    .如果真 (大于 (加载结果, 0))
        X64_枚举进程导出函数 (全_精易进程结构.进程句柄, 加载结果, 枚举结果)
        程序_延时 (500, )
        ' 信息框 (“枚举结果 ” ＋ 到文本 (取数组成员数 (枚举结果)), 0, , )
        .计次循环首 (取数组成员数 (枚举结果), i)
            .判断开始 (等于 (枚举结果[i].函数名称, “VMMDLL_Initialize”))
                赋值 (全_函数入口.VMMDLL_Initialize, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_CloseAll”))
                赋值 (全_函数入口.VMMDLL_CloseAll, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_PidGetFromName”))
                赋值 (全_函数入口.VMMDLL_PidGetFromName, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ProcessGetModuleBaseU”))
                赋值 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Initialize”))
                赋值 (全_函数入口.VMMDLL_Scatter_Initialize, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Prepare”))
                赋值 (全_函数入口.VMMDLL_Scatter_Prepare, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_ExecuteRead”))
                赋值 (全_函数入口.VMMDLL_Scatter_ExecuteRead, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_CloseHandle”))
                赋值 (全_函数入口.VMMDLL_Scatter_CloseHandle, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Clear”))
                赋值 (全_函数入口.VMMDLL_Scatter_Clear, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_PidList”))
                赋值 (全_函数入口.VMMDLL_PidList, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ProcessGetInformation”))
                赋值 (全_函数入口.VMMDLL_ProcessGetInformation, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ConfigSet”))
                赋值 (全_函数入口.VMMDLL_ConfigSet, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_MemReadEx”))
                赋值 (全_函数入口.VMMDLL_MemReadEx, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Read”))
                赋值 (全_函数入口.VMMDLL_Scatter_Read, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ProcessGetModuleBaseU”))
                赋值 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_MemWrite”))
                赋值 (全_函数入口.VMMDLL_MemWrite, 枚举结果[i].函数入口)
            .默认
                
            .判断结束
            
        .计次循环尾 ()
    .如果真结束
    
.如果真结束
L_程序_调用子程序 (全局_状态回调, , , 参试次数, 1, , , , , , , , , , , , , )
返回 (大于 (取数组成员数 (枚举结果), 0))

.子程序 加载支持库, 长整数型, , 
.局部变量 模块句柄, 长整数型, , , 
.局部变量 内存地址, 长整数型, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 函数入口, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 官库名称, 文本型, , , 

.如果真 (等于 (全_精易进程结构.进程句柄, 0))
    返回 (0)
.如果真结束
赋值 (模块句柄, X64_取进程模块句柄 (全_精易进程结构.进程句柄, “kernel32.dll”))
.如果真 (等于 (模块句柄, 0))
    返回 (0)
.如果真结束
赋值 (函数入口, X64_取模块函数入口 (全_精易进程结构.进程句柄, 模块句柄, “LoadLibraryA”))
.如果真 (等于 (模块句柄, 0))
    返回 (0)
.如果真结束
赋值 (官库名称, “VMM.dll”)  ' VMM.dll
赋值 (内存地址, X64_申请内存 (全_精易进程结构.进程句柄, 0, 取字节集长度 (到字节集 (官库名称)), 位或 (4096, 8192), 64))
内存64_写文本型 (全_精易进程结构.进程句柄, 内存地址, 官库名称)
赋值 (寄存器64.rcx, 内存地址)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 函数入口, 寄存器64, , , ))
.如果真 (小于 (调用结果, 0))
    赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 函数入口, 寄存器64, , , ))
.如果真结束
X64_释放内存 (全_精易进程结构.进程句柄, 内存地址, 0, 32768)
返回 (调用结果)



.子程序 固定申请内存, 长整数型, , 
.参数 长度, 整数型, , 

返回 (X64_申请内存 (全_精易进程结构.进程句柄, 0, 长度, 位或 (4096, 8192), 64))

.子程序 获取结果, 逻辑型, , 
.参数 m, 整数型, , 
.参数 发送内容, 文本型, , 
.参数 结果, 字节集, 参考, 
.局部变量 发送结果, 整数型, , , 
.局部变量 实际读取长度, 整数型, , , 
.局部变量 开始时间, 整数型, , , 
.局部变量 写入结果, 整数型, , , 
.局部变量 读取结果, 整数型, , , 
.局部变量 已读字节, 整数型, , , 
.局部变量 总可用字节, 整数型, , , 
.局部变量 剩余字节, 整数型, , , 

.局部变量 重试次数, 整数型, , , 
.局部变量 最大重试次数, 整数型, , , 
.局部变量 读取字节集, 字节集, , , 
.局部变量 超时时间, 整数型, , , 

进入许可区 (许可证_管道)
赋值 (结果, 到字节集 (发送内容))
赋值 (写入结果, -1)
赋值 (重试次数, 0)
赋值 (最大重试次数, 3)
赋值 (超时时间, 1000)
' ------------ 写入（带超时与重试）
.判断循环首 (并且 (等于 (写入结果, -1), 小于 (重试次数, 最大重试次数)))
    赋值 (写入结果, WriteFileWithTimeout (m, 结果, 取字节集长度 (结果), 发送结果, 0, 超时时间))
    .如果真 (等于 (写入结果, 0))  ' 写入失败
        调试输出 (相加 (“写入失败，错误码：”, 到文本 (取最后错误 ())))
        赋值 (重试次数, 相加 (重试次数, 1))
        程序_延时 (100, )  ' 短暂延时后重试
    .如果真结束
    
.判断循环尾 ()
.如果真 (等于 (写入结果, 0))  ' 多次重试后仍失败
    调试输出 (“多次写入失败，退出”)
    退出许可区 (许可证_管道)
    返回 (假)
    
.如果真结束
' ------------ 读取（带超时、重试、缓冲区优化）
赋值 (开始时间, 取启动时间_Ex ())
赋值 (读取字节集, 取空白字节集 (4096))  ' 固定缓冲区大小，也可动态调整
赋值 (读取结果, -1)
赋值 (实际读取长度, 0)
赋值 (已读字节, 0)
赋值 (总可用字节, 0)
赋值 (剩余字节, 0)
赋值 (重试次数, 0)
.判断循环首 (小于 (相减 (取启动时间_Ex (), 开始时间), 1200))  ' 3000
    PeekNamedPipe (m, {}, 0, 已读字节, 总可用字节, 剩余字节)
    ' 调试输出 (已读字节, 总可用字节, 剩余字节)
    .如果真 (大于 (总可用字节, 0))
        赋值 (读取结果, ReadFileWithTimeout (m, 取变量数据地址 (读取字节集), 4096, 实际读取长度, 0, 超时时间))
        .如果 (等于 (读取结果, 0))  ' 读取失败
            调试输出 (相加 (“读取失败，错误码：”, 到文本 (取最后错误 ())))
            赋值 (重试次数, 相加 (重试次数, 1))
            .如果真 (大于或等于 (重试次数, 最大重试次数))
                调试输出 (“读取多次失败，退出”)
                退出许可区 (许可证_管道)
                返回 (假)
            .如果真结束
            ' 继续判断循环
        .否则
            .如果真 (等于 (实际读取长度, 0))
                调试输出 (“获取结果为0”)
                退出许可区 (许可证_管道)
                返回 (假)
            .如果真结束
            .如果 (不等于 (相减 (实际读取长度, 1), 0))
                赋值 (结果, 取字节集左边 (读取字节集, 相减 (实际读取长度, 1)))
                退出许可区 (许可证_管道)
                返回 (真)
            .否则
                退出许可区 (许可证_管道)
                返回 (假)
            .如果结束
            
        .如果结束
        
    .如果真结束
    ' 延时 (50)  ' 避免CPU空转
.判断循环尾 ()
调试输出 (“读取超时或未匹配，退出”)
退出许可区 (许可证_管道)
返回 (假)  ' 循环超时或未匹配结果

.子程序 拼接参数, 文本型, , 
.参数 方法名, 文本型, , 
.参数 value1, 文本型, 可空, 
.参数 value2, 文本型, 可空, 
.参数 value3, 文本型, 可空, 
.参数 value4, 文本型, 可空, 
.局部变量 str, 文本型, , , 

赋值 (str, 方法名)
.如果真 (取反 (是否为空 (value1)))
    赋值 (str, 相加 (str, “,”, value1))
.如果真结束
.如果真 (取反 (是否为空 (value2)))
    赋值 (str, 相加 (str, “,”, value2))
.如果真结束
.如果真 (取反 (是否为空 (value3)))
    赋值 (str, 相加 (str, “,”, value3))
.如果真结束
.如果真 (取反 (是否为空 (value4)))
    赋值 (str, 相加 (str, “,”, value4))
.如果真结束
返回 (str)

.子程序 WriteFileWithTimeout, 整数型, , 
.参数 hFile, 整数型, , 备注：文件句柄
.参数 lpBuffer, 字节集, , 备注：接收读取数据的缓冲区
.参数 nNumberOfBytesToWrite, 整数型, , 备注：要写入的字节数
.参数 lpNumberOfBytesWritten, 整数型, 参考, 备注：实际写入的字节数
.参数 lpOverlapped, 整数型, , 备注：OVERLAPPED 结构指针（异步操作需有效）
.参数 dwTimeout, 整数型, , 备注：超时时间（毫秒）
.局部变量 hEvent, 整数型, , , 
.局部变量 ret, 整数型, , , 
.局部变量 waitRet, 整数型, , , 

赋值 (hEvent, CreateEvent (0, 假, 假, 相加 (“Write”, 全局_特定随机值)))  ' 创建自动重置事件
.如果真 (等于 (hEvent, 0))
    返回 (0)
.如果真结束
' 异步写入（假设lpOverlapped有效，否则需构造OVERLAPPED结构）
赋值 (ret, WriteFile (hFile, lpBuffer, nNumberOfBytesToWrite, lpNumberOfBytesWritten, lpOverlapped))
.如果真 (并且 (等于 (ret, 0), 等于 (取最后错误 (), 997)))  ' ERROR_IO_PENDING，操作挂起
    赋值 (waitRet, WaitForSingleObject (hEvent, dwTimeout))  ' 等待事件或超时
    .如果 (等于 (waitRet, 0))  ' 事件触发（写入完成）
        GetOverlappedResult (hFile, lpOverlapped, lpNumberOfBytesWritten, 假)
        赋值 (ret, 1)
    .否则
        CancelIo (hFile)  ' 取消I/O（需声明CancelIo API）
        赋值 (ret, 0)
    .如果结束
    
.如果真结束
CloseHandle (hEvent)  ' 关闭事件句柄（需声明CloseHandle API）
返回 (ret)

.子程序 ReadFileWithTimeout, 整数型, , ' 实现逻辑与WriteFileWithTimeout类似，需处理读操作的异步与超时
.参数 hFile, 整数型, , 备注：文件句柄
.参数 lpBuffer, 整数型, , 备注：接收读取数据的缓冲区
.参数 nNumberOfBytesToRead, 整数型, , 备注：要读取的字节数
.参数 lpNumberOfBytesRead, 整数型, 参考, 备注：实际读取的字节数
.参数 lpOverlapped, 整数型, , 备注：OVERLAPPED 结构指针（异步操作需有效）
.参数 dwTimeout, 整数型, , 备注：超时时间（毫秒）
.局部变量 hEvent, 整数型, , , 
.局部变量 ret, 整数型, , , 
.局部变量 waitRet, 整数型, , , 

赋值 (hEvent, CreateEvent (0, 假, 假, 相加 (“Read”, 全局_特定随机值)))  ' 1. 创建自动重置事件
.如果真 (等于 (hEvent, 0))
    返回 (0)
.如果真结束
' 2. 异步读取
赋值 (ret, 选择 (ReadFile (hFile, lpBuffer, nNumberOfBytesToRead, lpNumberOfBytesRead, lpOverlapped), 1, 0))
.如果真 (并且 (等于 (ret, 0), 等于 (取最后错误 (), 997)))  ' ERROR_IO_PENDING，操作挂起
    ' 3. 等待事件触发或超时
    赋值 (waitRet, WaitForSingleObject (hEvent, dwTimeout))
    .如果 (等于 (waitRet, 0))  ' 事件触发（读取完成）
        GetOverlappedResult (hFile, lpOverlapped, lpNumberOfBytesRead, 假)
        赋值 (ret, 1)
    .否则
        CancelIo (hFile)  ' 取消IO操作（需确保已声明 CancelIo API）
        赋值 (ret, 0)
    .如果结束
    
.如果真结束
CloseHandle (hEvent)  ' 4. 关闭事件句柄（需声明 CloseHandle API）
返回 (ret)


