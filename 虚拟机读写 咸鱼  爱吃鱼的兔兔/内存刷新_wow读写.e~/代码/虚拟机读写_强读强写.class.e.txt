.程序集 虚拟机读写_强读强写, ,公开 , 
.程序集变量 集_基本路径, 文本型, , , 
.程序集变量 集_官方Vmm路径, 文本型, , , 
.程序集变量 集_空程序路径, 文本型, , , 
.程序集变量 集_官方COL路径, 文本型, , , 

.程序集变量 全_精易进程结构, 精易_进程结构, , , 
.程序集变量 全_函数入口, VMM函数入口, , , 

.程序集变量 集_VMM句柄, 长整数型, , , 
.程序集变量 集_进程ID, 长整数型, , , 
.程序集变量 集_散读ID, 长整数型, , , 
.程序集变量 集_散读进程ID, 长整数型, , , scatter handle 初始化时绑定的 PID
.程序集变量 集_操作锁, 整数型, , , 每个虚拟机实例独立串行化远程调用
.程序集变量 集_散读锁定, 逻辑型, , , 散读初始化到重置期间保持互斥体所有权
.程序集变量 集_散读线程ID, 整数型, , , 只有开始批次的线程可以操作和重置该批次
.程序集变量 集_锁已废弃, 逻辑型, , , 持锁线程被强制结束后禁止继续调用旧VMM
.程序集变量 集_已登记自删, 逻辑型, , , 每个实例只登记一次，避免注册表删除队列重复增长
.程序集变量 集_远程缓冲地址, 长整数型, , , 复用桥接进程缓冲，避免每次读取都 VirtualAlloc/VirtualFree
.程序集变量 集_远程缓冲长度, 整数型, , , 按 64KB 递增，只在当前实例操作锁内访问
.程序集变量 集_散读输出地址, 长整数型, , , PrepareEx 专用远程缓冲，必须保持到 scatter handle 关闭
.程序集变量 集_散读输出长度, 整数型, , , 固定 1MB，覆盖当前最多 500 个对象的批量读取
.程序集变量 集_散读请求地址, 长整数型, , "0", 
.程序集变量 集_散读请求长度, 整数型, , "0", 
.程序集变量 集_散读请求偏移, 整数型, , "0", 
.程序集变量 集_散读请求总长, 整数型, , , 
.程序集变量 集_散读计数偏移, 整数型, , , 
.程序集变量 集_散读本地结果, 字节集, , , ExecuteRead 后一次性从桥接进程拷回
.程序集变量 集_散读扩展已执行, 逻辑型, , , 
.程序集变量 集_散读缓存地址, 长整数型, , "0", 上一批已提交给 scatter handle 的地址
.程序集变量 集_散读缓存长度, 整数型, , "0", 
.程序集变量 集_散读缓存偏移, 整数型, , "0", PrepareEx 输出缓冲中的固定偏移
.程序集变量 集_散读缓存有效, 逻辑型, , , 地址列表不变时直接重复 ExecuteRead


.子程序 _初始化, , , 当基于本类的对象被创建后，此方法会被自动调用
.局部变量 随机数, 整数型, , , 
.局部变量 随机名, 文本型, , , 

置随机数种子 ()
赋值 (随机数, 取随机数 (5000, 8000))
赋值 (随机名, 相加 (到文本 (取启动时间_Ex ()), 到文本 (随机数)))
赋值 (集_基本路径, 相加 (取特定目录 (#临时文件目录), “ZCK”, 随机名))
赋值 (集_官方Vmm路径, 相加 (集_基本路径, “\vmm.dll”))
赋值 (集_官方COL路径, 相加 (集_基本路径, “\leechcore.dll”))
赋值 (集_空程序路径, 相加 (集_基本路径, “\Microsoft”, 随机名, “.exe”))
赋值 (集_操作锁, CreateMutexA (0, 假, 0))


.子程序 _销毁, , , 当基于本类的对象被销毁前，此方法会被自动调用

' 本类按值复制时会共享句柄，不能在析构时自动关闭。
' 资源由持有对象在线程停止后显式调用“关闭读写”回收。


.子程序 内部_加锁, 逻辑型, , 
.局部变量 等待结果, 整数型, , , 

.如果真 (等于 (集_操作锁, 0))
    返回 (假)
.如果真结束
赋值 (等待结果, WaitForSingleObject (集_操作锁, 10000))
.如果真 (等于 (等待结果, 128))
    赋值 (集_锁已废弃, 真)
    返回 (真)
.如果真结束
.如果真 (等于 (等待结果, 0))
    赋值 (集_锁已废弃, 假)
    返回 (真)
.如果真结束
返回 (假)

.子程序 内部_解锁, 逻辑型, , 

.如果真 (等于 (集_操作锁, 0))
    返回 (假)
.如果真结束
返回 (ReleaseMutex (集_操作锁))

.子程序 内部_清空散读请求, , , 

清除数组 (集_散读请求地址)
清除数组 (集_散读请求长度)
清除数组 (集_散读请求偏移)
赋值 (集_散读请求总长, 0)
赋值 (集_散读计数偏移, 0)
赋值 (集_散读扩展已执行, 假)

.子程序 内部_清空散读缓存, , , 

清除数组 (集_散读缓存地址)
清除数组 (集_散读缓存长度)
清除数组 (集_散读缓存偏移)
赋值 (集_散读缓存有效, 假)

.子程序 内部_关闭散读, , , 
.局部变量 寄存器64, 寄存器64, , , 

.如果真 (并且 (大于 (集_散读ID, 0), 不等于 (全_精易进程结构.进程句柄, 0), 不等于 (全_函数入口.VMMDLL_Scatter_CloseHandle, 0)))
    赋值 (寄存器64.rcx, 集_散读ID)
    X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_CloseHandle, 寄存器64, , , 0)
.如果真结束
赋值 (集_散读ID, 0)
赋值 (集_散读进程ID, 0)
.如果真 (并且 (大于 (集_散读输出地址, 0), 不等于 (全_精易进程结构.进程句柄, 0)))
    ' PrepareEx 保存了输出指针，必须先关闭 scatter handle 再释放这块内存
    X64_释放内存 (全_精易进程结构.进程句柄, 集_散读输出地址, 0, 32768)
.如果真结束
赋值 (集_散读输出地址, 0)
赋值 (集_散读输出长度, 0)
内部_清空散读请求 ()
内部_清空散读缓存 ()
赋值 (集_散读本地结果, {})

.子程序 内部_关闭VMM, , , 
.局部变量 寄存器64, 寄存器64, , , 

内部_关闭散读 ()
.如果真 (并且 (大于 (集_VMM句柄, 0), 不等于 (全_精易进程结构.进程句柄, 0), 不等于 (全_函数入口.VMMDLL_CloseAll, 0)))
    X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_CloseAll, 寄存器64, , , 0)
.如果真结束
赋值 (集_VMM句柄, 0)
赋值 (集_进程ID, 0)

.子程序 内部_关闭辅助进程, , , 

.如果真 (不等于 (全_精易进程结构.线程句柄, 0))
    CloseHandle (全_精易进程结构.线程句柄)
    赋值 (全_精易进程结构.线程句柄, 0)
.如果真结束
.如果真 (不等于 (全_精易进程结构.进程句柄, 0))
    TerminateProcess (全_精易进程结构.进程句柄, 0)
    WaitForSingleObject (全_精易进程结构.进程句柄, 2000)
    CloseHandle (全_精易进程结构.进程句柄)
    赋值 (全_精易进程结构.进程句柄, 0)
.如果真结束
赋值 (全_精易进程结构.进程标识符, 0)
赋值 (全_精易进程结构.线程标识符, 0)
赋值 (集_散读ID, 0)
赋值 (集_散读进程ID, 0)
赋值 (集_VMM句柄, 0)
赋值 (集_进程ID, 0)
赋值 (集_散读锁定, 假)
赋值 (集_散读线程ID, 0)
赋值 (集_远程缓冲地址, 0)
赋值 (集_远程缓冲长度, 0)
赋值 (集_散读输出地址, 0)
赋值 (集_散读输出长度, 0)
内部_清空散读请求 ()
内部_清空散读缓存 ()
赋值 (集_散读本地结果, {})
赋值 (全_函数入口.VMMDLL_Initialize, 0)
赋值 (全_函数入口.VMMDLL_CloseAll, 0)
赋值 (全_函数入口.VMMDLL_PidGetFromName, 0)
赋值 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 0)
赋值 (全_函数入口.VMMDLL_Scatter_Initialize, 0)
赋值 (全_函数入口.VMMDLL_Scatter_Prepare, 0)
赋值 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0)
赋值 (全_函数入口.VMMDLL_Scatter_ExecuteRead, 0)
赋值 (全_函数入口.VMMDLL_Scatter_CloseHandle, 0)
赋值 (全_函数入口.VMMDLL_Scatter_Clear, 0)
赋值 (全_函数入口.VMMDLL_Scatter_Read, 0)
赋值 (全_函数入口.VMMDLL_PidList, 0)
赋值 (全_函数入口.VMMDLL_ProcessGetInformation, 0)
赋值 (全_函数入口.VMMDLL_ConfigSet, 0)
赋值 (全_函数入口.VMMDLL_MemReadEx, 0)
赋值 (全_函数入口.VMMDLL_MemWrite, 0)


.子程序 内部_初始化DMA设备, 逻辑型, , 
.参数 设备文本, 文本型, , 第三个参数
.局部变量 参数, 文本型, , "0", 
.局部变量 第一个参数, 文本型, , , 
.局部变量 第二个参数, 文本型, , , 
.局部变量 参数地址数组, 长整数型, , "3", 
.局部变量 临时内存地址, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 DLL初始化结果, 逻辑型, , , 

.计次循环首 (10, )
    赋值 (DLL初始化结果, 内部_初始化VMware (1))  ' 此处可能会失败，多尝试几次
    .如果真 (DLL初始化结果)
        跳出循环 ()
    .如果真结束
    ' 每次失败都结束本次桥接，防止残留进程每毫秒唤醒并继续占用句柄
    内部_关闭辅助进程 ()
    程序_延时 (50, 0)
.计次循环尾 ()
.如果真 (取反 (DLL初始化结果))
    返回 (假)
.如果真结束
赋值 (第一个参数, “”)
赋值 (第二个参数, “-device”)
.如果真 (等于 (全_函数入口.VMMDLL_Initialize, 0))
    信息框 (相加 (“VMMDLL_Initialize ”, 到文本 (全_函数入口.VMMDLL_Initialize)), 0, , )
    返回 (假)
.如果真结束
赋值 (参数地址数组[1], 内部_固定申请内存 (256))
赋值 (参数地址数组[2], 内部_固定申请内存 (相加 (取文本长度 (第二个参数), 1)))
赋值 (参数地址数组[3], 内部_固定申请内存 (相加 (取文本长度 (设备文本), 1)))
.如果真 (或者 (等于 (参数地址数组[1], 0), 等于 (参数地址数组[2], 0), 等于 (参数地址数组[3], 0)))
    返回 (假)
.如果真结束
.如果真 (取反 (内存64_写文本型 (全_精易进程结构.进程句柄, 参数地址数组[1], 第一个参数)))
    返回 (假)
.如果真结束
.如果真 (取反 (内存64_写文本型 (全_精易进程结构.进程句柄, 参数地址数组[2], 第二个参数)))
    返回 (假)
.如果真结束
.如果真 (取反 (内存64_写文本型 (全_精易进程结构.进程句柄, 参数地址数组[3], 设备文本)))
    返回 (假)
.如果真结束
赋值 (临时内存地址, 内部_固定申请内存 (24))  ' 24
.如果真 (等于 (临时内存地址, 0))
    返回 (假)
.如果真结束
内存64_写长整数型 (全_精易进程结构.进程句柄, 临时内存地址, 参数地址数组[1])
内存64_写长整数型 (全_精易进程结构.进程句柄, 相加 (临时内存地址, 8), 参数地址数组[2])
内存64_写长整数型 (全_精易进程结构.进程句柄, 相加 (临时内存地址, 16), 参数地址数组[3])
赋值 (寄存器64.rcx, 3)
赋值 (寄存器64.rdx, 临时内存地址)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Initialize, 寄存器64, , , 0))
X64_释放内存 (全_精易进程结构.进程句柄, 参数地址数组[1], 0, 32768)
X64_释放内存 (全_精易进程结构.进程句柄, 参数地址数组[2], 0, 32768)
X64_释放内存 (全_精易进程结构.进程句柄, 参数地址数组[3], 0, 32768)
X64_释放内存 (全_精易进程结构.进程句柄, 临时内存地址, 0, 32768)
.如果 (大于 (调用结果, 0))
    赋值 (集_VMM句柄, 调用结果)
.否则
    赋值 (集_VMM句柄, 0)
.如果结束
调试输出 (“读写模块_虚拟机句柄 = ”, 集_VMM句柄)
调试输出 (“读写模块_进程句柄 = ”, 全_精易进程结构.进程句柄)
返回 (大于 (集_VMM句柄, 0))

.子程序 内部_初始化VMware, 逻辑型, , 
.参数 尝试次数, 整数型, , 
.局部变量 创建结果, 整数型, , , 
.局部变量 加载结果, 长整数型, , , 
.局部变量 枚举结果, 枚举导出函数, , "0", 
.局部变量 i, 整数型, , , 

.如果真 (等于 (help_初始化模块 (), 0))
    信息框 (“X64模块初始化失败！”, 相加 (0, #错误图标, #位于前台), “错误：”, )
    返回 (假)
.如果真结束
.如果真 (取反 (文件是否存在 (集_基本路径)))
    创建目录 (集_基本路径)
    程序_延时 (300, )
.如果真结束
.如果真 (取反 (文件是否存在 (集_官方Vmm路径)))
    写到文件 (集_官方Vmm路径, #vmm)
    程序_延时 (300, )
.如果真结束
.如果真 (取反 (文件是否存在 (集_空程序路径)))
    写到文件 (集_空程序路径, #桥接)
    程序_延时 (300, )
.如果真结束
.如果真 (取反 (文件是否存在 (集_官方COL路径)))
    写到文件 (集_官方COL路径, #leechore)
    程序_延时 (300, )
.如果真结束
.如果真 (或者 (取反 (文件是否存在 (集_基本路径)), 取反 (文件是否存在 (集_官方Vmm路径)), 取反 (文件是否存在 (集_空程序路径)), 取反 (文件是否存在 (集_官方COL路径))))
    信息框 (“支持库释放失败！（请确保程序启动权限是否为管理员）”, 相加 (0, #错误图标, #位于前台), “错误：”, )
    返回 (假)
.如果真结束
.如果真 (取反 (集_已登记自删))
    ' 先登记文件再登记目录；每个实例只做一次，避免重试不断扩大系统删除队列
    MoveFileExA (集_官方Vmm路径, 0, 4)
    MoveFileExA (集_官方COL路径, 0, 4)
    MoveFileExA (集_空程序路径, 0, 4)
    MoveFileExA (集_基本路径, 0, 4)
    赋值 (集_已登记自删, 真)
.如果真结束
赋值 (创建结果, 进程_创建Ex (集_空程序路径, 相加 (“ ”, 到文本 (GetCurrentProcessId ())), 假, #隐藏窗口, 集_基本路径, 全_精易进程结构))
.如果真 (大于 (创建结果, 0))
    赋值 (加载结果, 内部_加载DLL ())
    .如果真 (大于 (加载结果, 0))
        X64_枚举进程导出函数 (全_精易进程结构.进程句柄, 加载结果, 枚举结果)
        .计次循环首 (取数组成员数 (枚举结果), i)
            .判断开始 (等于 (枚举结果[i].函数名称, “VMMDLL_Initialize”))
                赋值 (全_函数入口.VMMDLL_Initialize, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_CloseAll”))
                赋值 (全_函数入口.VMMDLL_CloseAll, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_PidGetFromName”))
                赋值 (全_函数入口.VMMDLL_PidGetFromName, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ProcessGetModuleBaseU”))
                赋值 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Initialize”))
                赋值 (全_函数入口.VMMDLL_Scatter_Initialize, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Prepare”))
                赋值 (全_函数入口.VMMDLL_Scatter_Prepare, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_PrepareEx”))
                赋值 (全_函数入口.VMMDLL_Scatter_PrepareEx, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_ExecuteRead”))
                赋值 (全_函数入口.VMMDLL_Scatter_ExecuteRead, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_CloseHandle”))
                赋值 (全_函数入口.VMMDLL_Scatter_CloseHandle, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Clear”))
                赋值 (全_函数入口.VMMDLL_Scatter_Clear, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_PidList”))
                赋值 (全_函数入口.VMMDLL_PidList, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ProcessGetInformation”))
                赋值 (全_函数入口.VMMDLL_ProcessGetInformation, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_ConfigSet”))
                赋值 (全_函数入口.VMMDLL_ConfigSet, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_MemReadEx”))
                赋值 (全_函数入口.VMMDLL_MemReadEx, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_Scatter_Read”))
                赋值 (全_函数入口.VMMDLL_Scatter_Read, 枚举结果[i].函数入口)
            .判断 (等于 (枚举结果[i].函数名称, “VMMDLL_MemWrite”))
                赋值 (全_函数入口.VMMDLL_MemWrite, 枚举结果[i].函数入口)
            .默认
                
            .判断结束
            
        .计次循环尾 ()
    .如果真结束
    
.如果真结束
.如果真 (不等于 (全_精易进程结构.线程句柄, 0))
    CloseHandle (全_精易进程结构.线程句柄)
    赋值 (全_精易进程结构.线程句柄, 0)
.如果真结束
返回 (并且 (不等于 (全_函数入口.VMMDLL_Initialize, 0), 不等于 (全_函数入口.VMMDLL_CloseAll, 0), 不等于 (全_函数入口.VMMDLL_PidGetFromName, 0), 不等于 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 0), 不等于 (全_函数入口.VMMDLL_MemReadEx, 0), 不等于 (全_函数入口.VMMDLL_MemWrite, 0), 不等于 (全_函数入口.VMMDLL_Scatter_Initialize, 0), 不等于 (全_函数入口.VMMDLL_Scatter_Prepare, 0), 不等于 (全_函数入口.VMMDLL_Scatter_ExecuteRead, 0), 不等于 (全_函数入口.VMMDLL_Scatter_Read, 0), 不等于 (全_函数入口.VMMDLL_Scatter_Clear, 0), 不等于 (全_函数入口.VMMDLL_Scatter_CloseHandle, 0)))

.子程序 内部_加载DLL, 长整数型, , 
.局部变量 模块句柄, 长整数型, , , 
.局部变量 内存地址, 长整数型, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 函数入口, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 官库名称, 文本型, , , 

.如果真 (等于 (全_精易进程结构.进程句柄, 0))
    返回 (0)
.如果真结束
赋值 (模块句柄, X64_取进程模块句柄 (全_精易进程结构.进程句柄, “kernel32.dll”))
.如果真 (等于 (模块句柄, 0))
    返回 (0)
.如果真结束
赋值 (函数入口, X64_取模块函数入口 (全_精易进程结构.进程句柄, 模块句柄, “LoadLibraryA”))
.如果真 (等于 (函数入口, 0))
    返回 (0)
.如果真结束
赋值 (官库名称, “vmm.dll”)  ' VMM.dll
赋值 (内存地址, X64_申请内存 (全_精易进程结构.进程句柄, 0, 相加 (取字节集长度 (到字节集 (官库名称)), 1), 位或 (4096, 8192), 4))
.如果真 (等于 (内存地址, 0))
    返回 (0)
.如果真结束
内存64_写文本型 (全_精易进程结构.进程句柄, 内存地址, 官库名称)
赋值 (寄存器64.rcx, 内存地址)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 函数入口, 寄存器64, , , 0))
.如果真 (等于 (调用结果, 0))
    赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 函数入口, 寄存器64, , , 0))
.如果真结束
X64_释放内存 (全_精易进程结构.进程句柄, 内存地址, 0, 32768)
返回 (调用结果)

.子程序 内部_固定申请内存, 长整数型, , 
.参数 长度, 整数型, , 

返回 (X64_申请内存 (全_精易进程结构.进程句柄, 0, 长度, 位或 (4096, 8192), 4))

.子程序 内部_取复用缓冲, 长整数型, , 只允许在当前实例操作锁内调用
.参数 所需长度, 整数型, , 
.局部变量 新长度, 整数型, , ,

.如果真 (或者 (小于或等于 (所需长度, 0), 大于 (所需长度, 268435456), 等于 (全_精易进程结构.进程句柄, 0)))
    返回 (0)
.如果真结束
.如果真 (并且 (大于 (集_远程缓冲地址, 0), 大于或等于 (集_远程缓冲长度, 所需长度)))
    返回 (集_远程缓冲地址)
.如果真结束
.如果真 (大于 (集_远程缓冲地址, 0))
    X64_释放内存 (全_精易进程结构.进程句柄, 集_远程缓冲地址, 0, 32768)
    赋值 (集_远程缓冲地址, 0)
    赋值 (集_远程缓冲长度, 0)
.如果真结束
' 64KB 分级增长；怪物与常规属性读取通常终生只申请一次
赋值 (新长度, 相乘 (整除 (相加 (所需长度, 65535), 65536), 65536))
赋值 (集_远程缓冲地址, 内部_固定申请内存 (新长度))
.如果 (大于 (集_远程缓冲地址, 0))
    赋值 (集_远程缓冲长度, 新长度)
.否则
    赋值 (集_远程缓冲地址, 0)
    赋值 (集_远程缓冲长度, 0)
.如果结束
返回 (集_远程缓冲地址)

.子程序 内部_取散读输出缓冲, 长整数型, , PrepareEx 输出地址在 scatter handle 关闭前保持不变
.参数 所需长度, 整数型, , 

.如果真 (或者 (小于或等于 (所需长度, 0), 大于 (所需长度, 1048576), 等于 (全_精易进程结构.进程句柄, 0)))
    返回 (0)
.如果真结束
.如果真 (大于 (集_散读输出地址, 0))
    .如果 (大于或等于 (集_散读输出长度, 所需长度))
        返回 (集_散读输出地址)
    .否则
        ' 已提交给 PrepareEx 的指针不能扩容释放；超出固定容量时让本批失败
        返回 (0)
    .如果结束
.如果真结束
赋值 (集_散读输出地址, 内部_固定申请内存 (1048576))
.如果 (大于 (集_散读输出地址, 0))
    赋值 (集_散读输出长度, 1048576)
.否则
    赋值 (集_散读输出地址, 0)
    赋值 (集_散读输出长度, 0)
.如果结束
返回 (集_散读输出地址)

.子程序 内部_添加扩展请求, 逻辑型, , PrepareEx 模式只登记本地描述符
.参数 数据地址, 长整数型, , 
.参数 数据长度, 整数型, , 
.局部变量 新总长, 整数型, , , 
.局部变量 新数量, 整数型, , , 
.局部变量 新计数偏移, 整数型, , , 

.如果真 (或者 (小于 (数据地址, 4194304), 小于或等于 (数据长度, 0), 大于 (数据长度, 1048576)))
    返回 (假)
.如果真结束
.如果真 (大于 (集_散读请求总长, 相减 (1048576, 数据长度)))
    返回 (假)
.如果真结束
赋值 (新总长, 相加 (集_散读请求总长, 数据长度))
赋值 (新数量, 相加 (取数组成员数 (集_散读请求地址), 1))
赋值 (新计数偏移, 相乘 (整除 (相加 (新总长, 7), 8), 8))
.如果真 (大于 (相加 (新计数偏移, 相乘 (新数量, 4)), 1048576))
    返回 (假)
.如果真结束
' 添加新请求后，必须重新执行才能读取本批快照
赋值 (集_散读扩展已执行, 假)
加入成员 (集_散读请求地址, 数据地址)
加入成员 (集_散读请求长度, 数据长度)
加入成员 (集_散读请求偏移, 集_散读请求总长)
赋值 (集_散读请求总长, 新总长)
返回 (真)

.子程序 内部_散读缓存匹配, 逻辑型, , 
.局部变量 i, 整数型, , , 
.局部变量 j, 整数型, , , 
.局部变量 数量, 整数型, , , 
.局部变量 请求出现次数, 整数型, , , 
.局部变量 缓存出现次数, 整数型, , , 

.如果真 (取反 (集_散读缓存有效))
    返回 (假)
.如果真结束
赋值 (数量, 取数组成员数 (集_散读请求地址))
.如果真 (或者 (不等于 (数量, 取数组成员数 (集_散读缓存地址)), 不等于 (数量, 取数组成员数 (集_散读缓存长度)), 不等于 (数量, 取数组成员数 (集_散读缓存偏移))))
    返回 (假)
.如果真结束
' 字典槽位换序不应导致整批重新 PrepareEx；同时按出现次数兼容重复范围
.计次循环首 (数量, i)
    赋值 (请求出现次数, 0)
    赋值 (缓存出现次数, 0)
    .计次循环首 (数量, j)
        .如果真 (并且 (等于 (集_散读请求地址[j], 集_散读请求地址[i]), 等于 (集_散读请求长度[j], 集_散读请求长度[i])))
            赋值 (请求出现次数, 相加 (请求出现次数, 1))
        .如果真结束
        .如果真 (并且 (等于 (集_散读缓存地址[j], 集_散读请求地址[i]), 等于 (集_散读缓存长度[j], 集_散读请求长度[i])))
            赋值 (缓存出现次数, 相加 (缓存出现次数, 1))
        .如果真结束
    .计次循环尾 ()
    .如果真 (不等于 (请求出现次数, 缓存出现次数))
        返回 (假)
    .如果真结束
.计次循环尾 ()
返回 (真)

.子程序 内部_保存散读缓存, , , 
.局部变量 i, 整数型, , , 

内部_清空散读缓存 ()
.计次循环首 (取数组成员数 (集_散读请求地址), i)
    加入成员 (集_散读缓存地址, 集_散读请求地址[i])
    加入成员 (集_散读缓存长度, 集_散读请求长度[i])
    加入成员 (集_散读缓存偏移, 集_散读请求偏移[i])
.计次循环尾 ()
赋值 (集_散读缓存有效, 真)

.子程序 内部_执行扩展缓存, 逻辑型, , 地址列表未变化时只执行一次 ExecuteRead
.局部变量 数量, 整数型, , , 
.局部变量 i, 整数型, , , 
.局部变量 使用长度, 整数型, , , 
.局部变量 输出基址, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 其余参数, 长整数型, , "1", 
.局部变量 调用结果, 长整数型, , , 
.局部变量 缓存匹配, 逻辑型, , , 
.局部变量 清理结果, 逻辑型, , , 
.局部变量 拷贝长度, 整数型, , , 
.局部变量 本地缓冲长度, 整数型, , , 

赋值 (集_散读扩展已执行, 假)
赋值 (数量, 取数组成员数 (集_散读请求地址))
.如果真 (或者 (等于 (数量, 0), 不等于 (数量, 取数组成员数 (集_散读请求长度)), 不等于 (数量, 取数组成员数 (集_散读请求偏移)), 等于 (集_散读ID, 0), 等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0)))
    返回 (假)
.如果真结束
赋值 (集_散读计数偏移, 相乘 (整除 (相加 (集_散读请求总长, 7), 8), 8))
赋值 (使用长度, 相加 (集_散读计数偏移, 相乘 (数量, 4)))
赋值 (输出基址, 内部_取散读输出缓冲 (使用长度))
.如果真 (等于 (输出基址, 0))
    返回 (假)
.如果真结束

赋值 (缓存匹配, 内部_散读缓存匹配 ())
.如果真 (取反 (缓存匹配))
    ' handle 中只保留一份稳定描述符；地址变化时先释放上一批内部范围
    .如果真 (集_散读缓存有效)
        赋值 (清理结果, 内部_重置缓存 ())
        .如果真 (取反 (清理结果))
            内部_关闭散读 ()
            返回 (假)
        .如果真结束
        内部_清空散读缓存 ()
    .如果真结束
    .计次循环首 (数量, i)
        赋值 (寄存器64.rcx, 集_散读ID)
        赋值 (寄存器64.rdx, 集_散读请求地址[i])
        赋值 (寄存器64.r8, 集_散读请求长度[i])
        赋值 (寄存器64.r9, 相加 (输出基址, 集_散读请求偏移[i]))
        赋值 (其余参数[1], 相加 (输出基址, 集_散读计数偏移, 相乘 (相减 (i, 1), 4)))
        赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_PrepareEx, 寄存器64, 其余参数, , 0))
        .如果真 (小于或等于 (调用结果, 0))
            赋值 (清理结果, 内部_重置缓存 ())
            内部_清空散读缓存 ()
            .如果真 (取反 (清理结果))
                内部_关闭散读 ()
            .如果真结束
            返回 (假)
        .如果真结束
    .计次循环尾 ()
.如果真结束

.如果真 (取反 (内部_刷新缓存 ()))
    赋值 (清理结果, 内部_重置缓存 ())
    内部_清空散读缓存 ()
    .如果真 (取反 (清理结果))
        内部_关闭散读 ()
    .如果真结束
    返回 (假)
.如果真结束
.如果真 (取反 (缓存匹配))
    内部_保存散读缓存 ()
.如果真结束

' 本地缓冲只增不缩，避免高频批次反复申请/释放造成堆抖动
.如果真 (小于 (取字节集长度 (集_散读本地结果), 使用长度))
    赋值 (本地缓冲长度, 相乘 (整除 (相加 (使用长度, 65535), 65536), 65536))
    赋值 (集_散读本地结果, 取空白字节集 (本地缓冲长度))
.如果真结束
赋值 (拷贝长度, 内存64_读字节集 (全_精易进程结构.进程句柄, 输出基址, 使用长度, 集_散读本地结果))
.如果真 (不等于 (拷贝长度, 使用长度))
    返回 (假)
.如果真结束
赋值 (集_散读扩展已执行, 真)
返回 (真)

.子程序 内部_读取扩展缓存, 字节集, , 
.参数 数据地址, 长整数型, , 
.参数 数据长度, 整数型, , 
.局部变量 i, 整数型, , , 
.局部变量 实际长度, 整数型, , , 
.局部变量 计数位置, 整数型, , , 
.局部变量 范围偏移, 整数型, , , 
.局部变量 当前批包含, 逻辑型, , , 

.如果真 (或者 (取反 (集_散读扩展已执行), 小于或等于 (数据长度, 0), 不等于 (取数组成员数 (集_散读请求地址), 取数组成员数 (集_散读请求长度)), 不等于 (取数组成员数 (集_散读请求地址), 取数组成员数 (集_散读缓存地址)), 不等于 (取数组成员数 (集_散读缓存地址), 取数组成员数 (集_散读缓存长度)), 不等于 (取数组成员数 (集_散读缓存地址), 取数组成员数 (集_散读缓存偏移))))
    返回 ({})
.如果真结束
' 只允许读取当前批添加过的范围，同时保留旧 Scatter_Read 的子区间能力
.计次循环首 (取数组成员数 (集_散读请求地址), i)
    .如果真 (并且 (大于或等于 (数据地址, 集_散读请求地址[i]), 小于或等于 (相加 (数据地址, 数据长度), 相加 (集_散读请求地址[i], 集_散读请求长度[i]))))
        赋值 (当前批包含, 真)
        跳出循环 ()
    .如果真结束
.计次循环尾 ()
.如果真 (取反 (当前批包含))
    返回 ({})
.如果真结束
.计次循环首 (取数组成员数 (集_散读请求地址), i)
    .如果真 (并且 (大于或等于 (数据地址, 集_散读缓存地址[i]), 小于或等于 (相加 (数据地址, 数据长度), 相加 (集_散读缓存地址[i], 集_散读缓存长度[i]))))
        赋值 (范围偏移, 相减 (数据地址, 集_散读缓存地址[i]))
        赋值 (计数位置, 相加 (集_散读计数偏移, 相乘 (相减 (i, 1), 4), 1))
        赋值 (实际长度, 取字节集数据 (集_散读本地结果, #整数型, 计数位置))
        ' pcbRead 是成功块累计值，不代表从首地址开始连续成功；整段完整才允许切片
        .如果真 (不等于 (实际长度, 集_散读缓存长度[i]))
            到循环尾 ()
        .如果真结束
        返回 (取字节集中间 (集_散读本地结果, 相加 (集_散读缓存偏移[i], 范围偏移, 1), 数据长度))
    .如果真结束
.计次循环尾 ()
返回 ({})


.子程序 内部_取进程Pid, 整数型, , 
.参数 进程名称, 文本型, , 
.局部变量 临时地址2, 长整数型, , , 
.局部变量 名称地址, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 返回值, 整数型, , , 

.如果真 (或者 (等于 (集_VMM句柄, 0), 等于 (全_精易进程结构.进程句柄, 0), 等于 (全_函数入口.VMMDLL_PidGetFromName, 0)))
    返回 (-1)
.如果真结束
赋值 (名称地址, 内部_固定申请内存 (256))
赋值 (临时地址2, 内部_固定申请内存 (4))
.如果真 (或者 (等于 (名称地址, 0), 等于 (临时地址2, 0)))
    .如果真 (不等于 (名称地址, 0))
        X64_释放内存 (全_精易进程结构.进程句柄, 名称地址, 0, 32768)
    .如果真结束
    .如果真 (不等于 (临时地址2, 0))
        X64_释放内存 (全_精易进程结构.进程句柄, 临时地址2, 0, 32768)
    .如果真结束
    返回 (-1)
.如果真结束
内存64_写文本型 (全_精易进程结构.进程句柄, 名称地址, 进程名称)
赋值 (寄存器64.rcx, 集_VMM句柄)
赋值 (寄存器64.rdx, 名称地址)
赋值 (寄存器64.r8, 临时地址2)
赋值 (集_进程ID, 0)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_PidGetFromName, 寄存器64, , , 0))
.如果真 (等于 (调用结果, 1))
    内存64_读整数型 (全_精易进程结构.进程句柄, 临时地址2, 集_进程ID)
.如果真结束
X64_释放内存 (全_精易进程结构.进程句柄, 名称地址, 0, 32768)
X64_释放内存 (全_精易进程结构.进程句柄, 临时地址2, 0, 32768)
调试输出 (相加 (“虚拟机指定进程id = ”, 到文本 (集_进程ID)))
.如果 (等于 (调用结果, 1))
    赋值 (返回值, 集_进程ID)
.否则
    赋值 (返回值, -1)
.如果结束
返回 (返回值)

.子程序 内部_读字节集, 字节集, , 
.参数 内存地址, 长整数型, , 
.参数 读取长度, 整数型, , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 其余参数, 长整数型, , "3", 
.局部变量 临时地址, 长整数型, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 pb, 字节集, , , 
.局部变量 计数地址, 长整数型, , , 
.局部变量 实际读取长度, 整数型, , , 
.局部变量 写入结果, 整数型, , , 

.如果真 (或者 (小于 (内存地址, 4194304), 小于或等于 (读取长度, 0), 大于 (读取长度, 268435452), 等于 (集_VMM句柄, 0), 等于 (集_进程ID, 0), 等于 (全_精易进程结构.进程句柄, 0)))
    返回 ({})
.如果真结束
.如果真 (等于 (全_函数入口.VMMDLL_MemReadEx, 0))
    返回 ({})
.如果真结束
赋值 (寄存器64.rcx, 集_VMM句柄)
赋值 (寄存器64.rdx, 集_进程ID)
赋值 (寄存器64.r8, 内存地址)
赋值 (临时地址, 内部_取复用缓冲 (相加 (读取长度, 4)))
.如果真 (等于 (临时地址, 0))
    返回 ({})
.如果真结束
赋值 (pb, 取空白字节集 (读取长度))
赋值 (计数地址, 相加 (临时地址, 读取长度))
赋值 (写入结果, 内存64_写字节集 (全_精易进程结构.进程句柄, 计数地址, 取空白字节集 (4)))
.如果真 (不等于 (写入结果, 4))
    返回 ({})
.如果真结束
赋值 (寄存器64.r9, 临时地址)
赋值 (其余参数[1], 读取长度)
赋值 (其余参数[2], 计数地址)
赋值 (其余参数[3], 1)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_MemReadEx, 寄存器64, 其余参数, , 0))
.如果真 (等于 (调用结果, 1))
    内存64_读整数型 (全_精易进程结构.进程句柄, 计数地址, 实际读取长度)
.如果真结束
.如果 (并且 (等于 (调用结果, 1), 等于 (实际读取长度, 读取长度)))
    内存64_读字节集 (全_精易进程结构.进程句柄, 临时地址, 读取长度, pb)
    返回 (pb)
.否则
    返回 ({})
.如果结束

.子程序 内部_初始化缓存, 逻辑型, , 
.局部变量 寄存器64, 寄存器64, , , 

.如果真 (大于 (集_散读ID, 0))
    返回 (真)
.如果真结束
赋值 (集_散读ID, 0)
.如果真 (或者 (等于 (集_VMM句柄, 0), 等于 (集_进程ID, 0), 等于 (全_精易进程结构.进程句柄, 0), 等于 (全_函数入口.VMMDLL_Scatter_Initialize, 0)))
    返回 (假)
.如果真结束
赋值 (寄存器64.rcx, 集_VMM句柄)
赋值 (寄存器64.rdx, 集_进程ID)
赋值 (寄存器64.r8, 1)
赋值 (集_散读ID, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_Initialize, 寄存器64, , , 0))
.如果真 (小于或等于 (集_散读ID, 0))
    赋值 (集_散读ID, 0)
    赋值 (集_散读进程ID, 0)
    返回 (假)
.如果真结束
赋值 (集_散读进程ID, 集_进程ID)
内部_清空散读缓存 ()
返回 (真)

.子程序 内部_散读字节集, 字节集, , 
.参数 数据地址, 长整数型, , 
.参数 数据长度, 整数型, , 
.局部变量 临时地址, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 pb, 字节集, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 其余参数, 长整数型, , "1", 
.局部变量 计数地址, 长整数型, , , 
.局部变量 实际读取长度, 整数型, , , 
.局部变量 写入结果, 整数型, , , 

.如果真 (或者 (等于 (集_散读ID, 0), 小于 (数据地址, 4194304), 小于或等于 (数据长度, 0), 大于 (数据长度, 268435452), 等于 (全_精易进程结构.进程句柄, 0), 等于 (全_函数入口.VMMDLL_Scatter_Read, 0)))
    返回 ({})
.如果真结束
赋值 (临时地址, 内部_取复用缓冲 (相加 (数据长度, 4)))
.如果真 (等于 (临时地址, 0))
    返回 ({})
.如果真结束
赋值 (计数地址, 相加 (临时地址, 数据长度))
赋值 (写入结果, 内存64_写字节集 (全_精易进程结构.进程句柄, 计数地址, 取空白字节集 (4)))
.如果真 (不等于 (写入结果, 4))
    返回 ({})
.如果真结束
赋值 (pb, 取空白字节集 (数据长度))
赋值 (寄存器64.rcx, 集_散读ID)
赋值 (寄存器64.rdx, 数据地址)
赋值 (寄存器64.r8, 数据长度)
赋值 (寄存器64.r9, 临时地址)
赋值 (其余参数[1], 计数地址)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_Read, 寄存器64, 其余参数, , 0))
.如果真 (等于 (调用结果, 1))
    内存64_读整数型 (全_精易进程结构.进程句柄, 计数地址, 实际读取长度)
.如果真结束
.如果 (并且 (等于 (调用结果, 1), 等于 (实际读取长度, 数据长度)))
    内存64_读字节集 (全_精易进程结构.进程句柄, 临时地址, 数据长度, pb)
    返回 (pb)
.否则
    返回 ({})
.如果结束

.子程序 内部_添加缓存, 逻辑型, , 
.参数 数据地址, 长整数型, , 
.参数 数据长度, 整数型, , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 调用结果, 长整数型, , , 

' 返回 (VMMDLL_Scatter_Prepare (hs, 数据地址, 数据长度))
.如果真 (或者 (等于 (集_散读ID, 0), 小于 (数据地址, 4194304), 小于或等于 (数据长度, 0), 等于 (全_函数入口.VMMDLL_Scatter_Prepare, 0)))
    返回 (假)
.如果真结束
赋值 (寄存器64.rcx, 集_散读ID)
赋值 (寄存器64.rdx, 数据地址)
赋值 (寄存器64.r8, 数据长度)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_Prepare, 寄存器64, , , 0))
返回 (等于 (调用结果, 1))

.子程序 内部_刷新缓存, 逻辑型, , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 调用结果, 逻辑型, , , 

' 返回 (VMMDLL_Scatter_ExecuteRead (hs))
.如果真 (或者 (等于 (集_散读ID, 0), 等于 (全_函数入口.VMMDLL_Scatter_ExecuteRead, 0)))
    返回 (假)
.如果真结束
赋值 (寄存器64.rcx, 集_散读ID)
赋值 (调用结果, 等于 (X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_ExecuteRead, 寄存器64, , , 0), 1))
返回 (调用结果)

.子程序 内部_重置缓存, 逻辑型, , 
.局部变量 寄存器64, 寄存器64, , , 

' 返回 (VMMDLL_Scatter_Clear (hs, PID, Flags))
.如果真 (或者 (等于 (集_散读ID, 0), 等于 (全_函数入口.VMMDLL_Scatter_Clear, 0)))
    返回 (假)
.如果真结束
赋值 (寄存器64.rcx, 集_散读ID)
赋值 (寄存器64.rdx, 集_进程ID)
赋值 (寄存器64.r8, 1)
返回 (等于 (X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_Scatter_Clear, 寄存器64, , , 0), 1))

.子程序 内部_写字节集, 逻辑型, , 
.参数 内存地址, 长整数型, , 
.参数 字节集, 字节集, , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 其余参数, 长整数型, , "3", 
.局部变量 临时地址, 长整数型, , , 
.局部变量 调用结果, 长整数型, , , 
.局部变量 pb, 字节集, , , 
.局部变量 结果, 整数型, , , 
.局部变量 写的结果, 整数型, , , 
.局部变量 写入长度, 整数型, , , 

赋值 (写入长度, 取字节集长度 (字节集))
.如果真 (或者 (小于 (内存地址, 4194304), 小于或等于 (写入长度, 0), 等于 (集_VMM句柄, 0), 等于 (集_进程ID, 0), 等于 (全_精易进程结构.进程句柄, 0), 等于 (全_函数入口.VMMDLL_MemWrite, 0)))
    返回 (假)
.如果真结束
赋值 (寄存器64.rcx, 集_VMM句柄)
赋值 (寄存器64.rdx, 集_进程ID)
赋值 (寄存器64.r8, 内存地址)

赋值 (临时地址, 内部_取复用缓冲 (写入长度))
.如果真 (等于 (临时地址, 0))
    返回 (假)
.如果真结束
赋值 (写的结果, 内存64_写字节集 (全_精易进程结构.进程句柄, 临时地址, 字节集))
.如果真 (不等于 (写的结果, 写入长度))
    返回 (假)
.如果真结束
赋值 (寄存器64.r9, 临时地址)

赋值 (其余参数[1], 写的结果)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_MemWrite, 寄存器64, 其余参数, , 0))
返回 (等于 (调用结果, 1))

.子程序 内部_取模块地址, 长整数型, , 
.参数 目标名称, 文本型, , 
.局部变量 名称地址, 长整数型, , , 
.局部变量 寄存器64, 寄存器64, , , 
.局部变量 调用结果, 长整数型, , , 

.如果真 (或者 (等于 (目标名称, “”), 等于 (集_VMM句柄, 0), 等于 (集_进程ID, 0), 等于 (全_精易进程结构.进程句柄, 0), 等于 (全_函数入口.VMMDLL_ProcessGetModuleBaseU, 0)))
    返回 (0)
.如果真结束
赋值 (名称地址, 内部_固定申请内存 (256))
.如果真 (等于 (名称地址, 0))
    返回 (0)
.如果真结束
内存64_写文本型 (全_精易进程结构.进程句柄, 名称地址, 目标名称)
赋值 (寄存器64.rcx, 集_VMM句柄)
赋值 (寄存器64.rdx, 集_进程ID)
赋值 (寄存器64.r8, 名称地址)
赋值 (调用结果, X64_远程调用函数 (全_精易进程结构.进程句柄, 全_函数入口.VMMDLL_ProcessGetModuleBaseU, 寄存器64, , , 0))
X64_释放内存 (全_精易进程结构.进程句柄, 名称地址, 0, 32768)
.如果 (大于 (调用结果, 0))
    返回 (调用结果)
.否则
    返回 (0)
.如果结束

.子程序 读写初始化, 逻辑型, 公开, 返回 指定进程名称的 进程id
.参数 虚拟机进程id, 整数型, , 
.参数 状态回调, 整数型, 可空, 
.局部变量 初始化结果, 逻辑型, , , 
.局部变量 需要额外解锁, 逻辑型, , , 原线程在未重置散读批次时主动重新初始化

' 全局_状态回调 ＝ 状态回调
.如果真 (等于 (集_操作锁, 0))
    返回 (假)
.如果真结束
赋值 (需要额外解锁, 并且 (集_散读锁定, 等于 (集_散读线程ID, GetCurrentThreadId ())))
.如果真 (取反 (内部_加锁 ()))
    返回 (假)
.如果真结束
.如果 (集_锁已废弃)
    ' 被强杀线程的远程调用状态未知，只结束桥接进程，不再调用旧VMMDLL函数
    赋值 (需要额外解锁, 假)
    内部_关闭辅助进程 ()
.否则
    内部_关闭VMM ()
    内部_关闭辅助进程 ()
.如果结束
赋值 (初始化结果, 内部_初始化DMA设备 (相加 (“vmware://id=”, 到文本 (虚拟机进程id))))  ' “vmware” 是调试的时候用 只能单开一个  多开命令 “vmware://id=44876” “vmware://id=指定虚拟机进程id”
.如果真 (取反 (初始化结果))
    内部_关闭VMM ()
    内部_关闭辅助进程 ()
.如果真结束
赋值 (集_锁已废弃, 假)
内部_解锁 ()
.如果真 (需要额外解锁)
    内部_解锁 ()
.如果真结束
返回 (初始化结果)

.子程序 关闭读写, 逻辑型, 公开, 关闭散读、VMM和桥接进程；实例互斥体保持到进程退出
.局部变量 已加锁, 逻辑型, , , 
.局部变量 需要额外解锁, 逻辑型, , , 原线程在未重置散读批次时主动关闭

.如果真 (等于 (集_操作锁, 0))
    内部_关闭辅助进程 ()
    返回 (真)
.如果真结束
赋值 (需要额外解锁, 并且 (集_散读锁定, 等于 (集_散读线程ID, GetCurrentThreadId ())))
赋值 (已加锁, 内部_加锁 ())
.如果真 (取反 (已加锁))
    ' 等待中的线程仍可能使用这些句柄，超时后绝不能并发销毁
    返回 (假)
.如果真结束
.如果 (集_锁已废弃)
    赋值 (需要额外解锁, 假)
    内部_关闭辅助进程 ()
.否则
    内部_关闭VMM ()
    内部_关闭辅助进程 ()
.如果结束
赋值 (集_散读锁定, 假)
赋值 (集_散读线程ID, 0)
赋值 (集_锁已废弃, 假)
内部_解锁 ()
.如果真 (需要额外解锁)
    内部_解锁 ()
.如果真结束
返回 (真)


.子程序 设置进程, 逻辑型, 公开, 
.参数 进程名称, 文本型, , DNF.exe
.参数 返回进程, 整数型, 参考 可空, 
.局部变量 设置结果, 逻辑型, , , 

.如果真 (取反 (内部_加锁 ()))
    返回 (假)
.如果真结束
.如果真 (集_锁已废弃)
    内部_关闭辅助进程 ()
    赋值 (集_锁已废弃, 假)
    赋值 (返回进程, 0)
    内部_解锁 ()
    返回 (假)
.如果真结束
赋值 (返回进程, 内部_取进程Pid (进程名称))
赋值 (设置结果, 大于 (返回进程, 5))
内部_解锁 ()
返回 (设置结果)

.子程序 读字节集, 字节集, 公开, 
.参数 读取地址, 长整数型, , 
.参数 读取长度, 整数型, , 
.局部变量 临时字节集, 字节集, , , 

.如果真 (取反 (内部_加锁 ()))
    返回 ({})
.如果真结束
.如果真 (集_锁已废弃)
    内部_关闭辅助进程 ()
    赋值 (集_锁已废弃, 假)
    内部_解锁 ()
    返回 ({})
.如果真结束
赋值 (临时字节集, 内部_读字节集 (读取地址, 读取长度))
内部_解锁 ()
返回 (临时字节集)

.子程序 写字节集, 逻辑型, 公开, 
.参数 写入地址, 长整数型, , 
.参数 字节集, 字节集, , 
.局部变量 写入结果, 逻辑型, , , 

.如果真 (取反 (内部_加锁 ()))
    返回 (假)
.如果真结束
.如果真 (集_锁已废弃)
    内部_关闭辅助进程 ()
    赋值 (集_锁已废弃, 假)
    内部_解锁 ()
    返回 (假)
.如果真结束
赋值 (写入结果, 内部_写字节集 (写入地址, 字节集))
内部_解锁 ()
返回 (写入结果)


.子程序 写整数型, 逻辑型, 公开, 
.参数 写入地址, 长整数型, , 
.参数 值, 整数型, , 
.局部变量 字节集, 字节集, , , 

赋值 (字节集, 指针到字节集 (取变量数据地址 (值), 4))
返回 (写字节集 (写入地址, 字节集))


.子程序 读小数型, 小数型, 公开, 
.参数 地址, 长整数型, , 

返回 (取字节集数据 (读字节集 (地址, 4), #小数型, ))



.子程序 读字节型, 字节型, 公开, 
.参数 地址, 长整数型, , 

返回 (取字节集数据 (读字节集 (地址, 1), #字节型, ))



.子程序 读整数型, 整数型, 公开, 
.参数 地址, 长整数型, , 

返回 (取字节集数据 (读字节集 (地址, 4), #整数型, ))



.子程序 读长整数, 长整数型, 公开, 
.参数 地址, 长整数型, , 

返回 (取字节集数据 (读字节集 (地址, 8), #长整数型, ))



.子程序 取模块地址, 长整数型, 公开, 
.参数 模块名称, 文本型, , 
.局部变量 模块地址, 长整数型, , , 

.如果真 (取反 (内部_加锁 ()))
    返回 (0)
.如果真结束
.如果真 (集_锁已废弃)
    内部_关闭辅助进程 ()
    赋值 (集_锁已废弃, 假)
    内部_解锁 ()
    返回 (0)
.如果真结束
赋值 (模块地址, 内部_取模块地址 (模块名称))
内部_解锁 ()
返回 (模块地址)


.子程序 散读初始化, 逻辑型, 公开, 
.局部变量 初始化结果, 逻辑型, , , 
.局部变量 当前线程ID, 整数型, , , 
.局部变量 重复批次, 逻辑型, , , 同一线程重复初始化时只保留原有的一层锁

赋值 (初始化结果, 真)
赋值 (当前线程ID, GetCurrentThreadId ())
赋值 (重复批次, 并且 (集_散读锁定, 等于 (集_散读线程ID, 当前线程ID)))
.如果真 (取反 (内部_加锁 ()))
    返回 (假)
.如果真结束
.如果真 (集_锁已废弃)
    内部_关闭辅助进程 ()
    赋值 (集_锁已废弃, 假)
    内部_解锁 ()
    返回 (假)
.如果真结束
.如果真 (重复批次)
    ' WaitForSingleObject 对同线程互斥体会递归计数，先释放本次新增的一层
    内部_解锁 ()
.如果真结束
' 切换 PID 时必须重建 handle；正常批次保留 PrepareEx 描述符供重复执行
.如果真 (并且 (不等于 (集_散读ID, 0), 不等于 (集_散读进程ID, 集_进程ID)))
    内部_关闭散读 ()
.如果真结束
.如果真 (并且 (重复批次, 不等于 (集_散读ID, 0), 等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0)))
    赋值 (初始化结果, 内部_重置缓存 ())
    .如果真 (取反 (初始化结果))
        内部_关闭散读 ()
    .如果真结束
.如果真结束
.如果真 (等于 (集_散读ID, 0))
    赋值 (初始化结果, 内部_初始化缓存 ())
.如果真结束
.如果真 (取反 (初始化结果))
    赋值 (集_散读锁定, 假)
    赋值 (集_散读线程ID, 0)
    内部_解锁 ()
    返回 (假)
.如果真结束
内部_清空散读请求 ()
赋值 (集_散读锁定, 真)
赋值 (集_散读线程ID, 当前线程ID)
返回 (真)

.子程序 散读添加, 逻辑型, 公开, 
.参数 地址, 长整数型, , 
.参数 长度, 整数型, , 

.如果真 (或者 (取反 (集_散读锁定), 不等于 (集_散读线程ID, GetCurrentThreadId ())))
    返回 (假)
.如果真结束
.如果 (不等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0))
    返回 (内部_添加扩展请求 (地址, 长度))
.否则
    返回 (内部_添加缓存 (地址, 长度))
.如果结束

.子程序 散读执行, 逻辑型, 公开, 

.如果真 (或者 (取反 (集_散读锁定), 不等于 (集_散读线程ID, GetCurrentThreadId ())))
    返回 (假)
.如果真结束
.如果 (不等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0))
    返回 (内部_执行扩展缓存 ())
.否则
    返回 (内部_刷新缓存 ())
.如果结束

.子程序 散读取字节集, 字节集, 公开, 
.参数 地址, 长整数型, , 
.参数 长度, 整数型, , 
.局部变量 读取结果, 字节集, , , 

.如果真 (或者 (取反 (集_散读锁定), 不等于 (集_散读线程ID, GetCurrentThreadId ())))
    返回 ({})
.如果真结束
.如果 (不等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0))
    赋值 (读取结果, 内部_读取扩展缓存 (地址, 长度))
.否则
    赋值 (读取结果, 内部_散读字节集 (地址, 长度))
.如果结束
.如果真 (不等于 (取字节集长度 (读取结果), 长度))
    ' 批次可整体成功但单项跨页短读；只对失败项直读重试
    赋值 (读取结果, 内部_读字节集 (地址, 长度))
.如果真结束
返回 (读取结果)

.子程序 散读重置, 逻辑型, 公开, 
.局部变量 重置结果, 逻辑型, , , 

.如果真 (或者 (取反 (集_散读锁定), 不等于 (集_散读线程ID, GetCurrentThreadId ())))
    返回 (假)
.如果真结束
.如果 (不等于 (全_函数入口.VMMDLL_Scatter_PrepareEx, 0))
    ' 保留已准备描述符；下一批地址相同就只调用 ExecuteRead
    赋值 (重置结果, 真)
    内部_清空散读请求 ()
.否则
    赋值 (重置结果, 内部_重置缓存 ())
    .如果真 (取反 (重置结果))
        内部_关闭散读 ()
    .如果真结束
.如果结束
赋值 (集_散读锁定, 假)
赋值 (集_散读线程ID, 0)
内部_解锁 ()
返回 (重置结果)

